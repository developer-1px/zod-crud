// React entrypoint. Keep this facade limited to React hooks; pure/headless
// APIs and document types live under `zod-crud`.

export { useJSONDocument } from "../application/react/useJSONDocument.js";
