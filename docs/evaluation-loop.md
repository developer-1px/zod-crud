# Evaluation Loop Ledger

Goal: run 100 evaluation -> execution -> scoring loops for the zod-crud docs and demos before release.

This ledger is the progress record. A loop only counts when all three parts are present:

- Evaluate: name the user-facing confusion or missing proof.
- Execute: change docs, demo, tests, or verification assets toward that finding.
- Score: record current evidence after the change.

## Score Axes

Each loop scores only the axis it tries to improve.

| Axis | Passing evidence |
| --- | --- |
| API correctness | Docs/demo use the current public API and avoid private modules or stale names. |
| Target clarity | Raw insertion pointers and `{ before | after | replace }` value-relative targets are not mixed. |
| Task discoverability | A user can find the action needed to verify a real editing task. |
| State observability | Selection, clipboard, result, state, and patch effects are inspectable after each action. |
| LLM copyability | An LLM can copy an example without adding commit-after-mutation or `{ at }` mistakes. |
| Runtime health | Typecheck, tests, build, or browser checks cover the changed surface. |
| Performance regression | Core editing workloads have repeatable local measurements and targeted tests for optimized paths. |

## Progress

138 / 100 loops complete.

| Loop | Evaluate | Execute | Score |
| --- | --- | --- | --- |
| 001 | The workbench used one `target` for both existing values and insertion positions. That made `paste({ after: target })`, `paste("/cards/-")`, schema insert checks, and move destinations hard to distinguish. Clipboard state was also only `set/empty`, so repeated copy/paste behavior could not be graded from the UI. | Split workbench state into `valueTarget` and `insertTarget`; routed value-relative actions through `{ after: valueTarget }`; routed insertion actions through raw `insertTarget`; added clipboard snapshot and result call panels; added copy-to-insert and payload-insert actions. | Target clarity: 2 -> 4. State observability: 2 -> 4. Evidence: `apps/site/src/playgrounds/InterfaceWorkbench.playground.tsx`; `npm run typecheck -w @zod-crud/site` passed. |
| 002 | The docs explained each facade, but did not give a compact domain action layer that combines `can*`, patch, duplicate, selection-derived copy, paste targets, and history. That leaves LLMs and users to infer composition rules. | Added a `작업 레이어 예시` section to the site API doc with `addCard`, `duplicateCard`, `copySelectedCardsTo`, `pastePayloadAfter`, and `undo`, including raw insertion pointer vs `{ after }` wording. | LLM copyability: 3 -> 4. API correctness: 4 -> 4. Evidence: `apps/site/src/docs/zod-crud-api.md`; `npm run typecheck -w @zod-crud/site`, `npm test -w @zod-crud/site`, `npm run build -w @zod-crud/site`, and `git diff --check` passed. |
| 003 | The workbench target split needed a regression guard. Without a test, future UI cleanup could collapse value and insertion targets back into one ambiguous control. | Updated the site component test to require accessible `value target` and `insert target` controls and to execute the `copy to insert` flow. | Runtime health: 3 -> 4. Target clarity evidence: stronger. Evidence: `apps/site/tests/interface-workbench.test.tsx`; `npm run typecheck -w @zod-crud/site`, `npm test -w @zod-crud/site`, `npm run build -w @zod-crud/site`, and `git diff --check` passed. |
| 004 | The workbench result panel used short labels such as `patch.add` and `clipboard.paste`, so it showed that an action ran but not the public API shape a user or LLM should copy. | Changed result `call` values across the workbench to public API snippets such as `doc.clipboard.paste({ after: pointer })`, `doc.clipboard.paste(pointer, { spread: true, rekey })`, `doc.canPastePayload(pointer, payload)`, and `doc.schema.accepts(pointer, payload, "insert")`; updated component tests to assert those snippets appear after representative actions. | LLM copyability: 4 -> 5 for demo actions. Evidence: `apps/site/src/playgrounds/InterfaceWorkbench.playground.tsx`; `apps/site/tests/interface-workbench.test.tsx`; `npm run typecheck -w @zod-crud/site` and `npm test -w @zod-crud/site -- interface-workbench` passed. |
| 005 | Users and LLMs still had to infer JSONPath dialect boundaries from examples. Prior blind results showed uncertainty around query syntax and whether JSONPath participates in mutation. | Added canonical JSONPath examples and an explicit non-goal: JSONPath finds values; patches are still built with returned JSON Pointers. Mirrored the note in the README and guarded the site doc text in the workbench test. | LLM copyability: 4 -> 5 for query-to-patch flow. API correctness evidence: stronger. Evidence: `apps/site/src/docs/zod-crud-api.md`; `packages/zod-crud/README.md`; `apps/site/tests/interface-workbench.test.tsx`; `npm run typecheck -w @zod-crud/site` and `npm test -w @zod-crud/site -- interface-workbench` passed. |
| 006 | Selection docs said selection is state, but did not clearly show how a multi-select becomes a clipboard source or when clipboard methods can use selection defaults. This left source ownership ambiguous. | Added site and README examples that derive `source` from `doc.selection?.selectedPointers`, pass it to `doc.clipboard.copy(source)` and `cut(source)`, and state that omitted source falls back to current selection while explicit sources are easier to audit. Guarded the site wording in component tests. | Task discoverability: 3 -> 4. State observability: 4 -> 4. Evidence: `apps/site/src/docs/zod-crud-api.md`; `packages/zod-crud/README.md`; `apps/site/tests/interface-workbench.test.tsx`; `npm test -w @zod-crud/site` passed. |
| 007 | Capability docs showed success/failure shape but did not show how schema validation failures become UI-ready messages. This made `canPastePayload` less useful as a preflight interface. | Added failed `canPastePayload` examples to the site doc and README using `schema_violation` and `violations`, and guarded the site example in the workbench test. | Task discoverability: 4 -> 5 for validation flows. API correctness evidence: stronger. Evidence: `apps/site/src/docs/zod-crud-api.md`; `packages/zod-crud/README.md`; `apps/site/tests/interface-workbench.test.tsx`; `npm test -w @zod-crud/site` passed. |
| 008 | Paste target and mutation result semantics are repeated across README, SPEC, site docs, and `llms.txt`. Without an automated consistency check, stale aliases like `{ at }` or `PasteMode` can re-enter one surface unnoticed. | Added a site docs consistency test that reads README, SPEC, site docs, and `llms.txt`, rejects legacy paste target aliases, requires current `{ after }` wording, and requires applied-result "do not commit again" semantics. Updated site Vitest include rules so `.test.ts` consistency tests actually run. | API correctness evidence: 4 -> 5 for docs consistency. Runtime health: 4 -> 5 for docs drift. Evidence: `apps/site/tests/docs-consistency.test.ts`; `apps/site/vitest.config.ts`; `npm test -w @zod-crud/site` passed with 2 files and 4 tests. |
| 009 | The docs were still organized mostly by API surface. A first-time user with a concrete task had to translate "I want to paste a payload" or "I want to undo" into the right facade. | Added task-entrypoint tables to the site doc and README mapping common editing tasks to `doc.patch`, `doc.duplicate`, `doc.query`, `selection -> clipboard`, `pastePayload`, `can*`, and history calls. Guarded the site table in component tests. | Task discoverability: 4 -> 5. LLM copyability: 5 -> 5. Evidence: `apps/site/src/docs/zod-crud-api.md`; `packages/zod-crud/README.md`; `apps/site/tests/interface-workbench.test.tsx`; `npm run typecheck -w @zod-crud/site`, `npm test -w @zod-crud/site`, `npm run build -w @zod-crud/site`, and `git diff --check` passed. |
| 010 | JSONPath boundaries were improved in README and site docs, but SPEC and `llms.txt` did not yet carry the same explicit "search only, patch with Pointer" rule. | Added the JSONPath search-only rule to SPEC and `llms.txt`, then extended the docs consistency test to require this rule across README, SPEC, site docs, and `llms.txt`. | API correctness evidence: 5 -> 5 for query/mutation boundary. Runtime health: 5 -> 5 for docs drift. Evidence: `packages/zod-crud/SPEC.md`; `llms.txt`; `apps/site/tests/docs-consistency.test.ts`; `npm run typecheck -w @zod-crud/site`, `npm test -w @zod-crud/site`, `npm run build -w @zod-crud/site`, and `git diff --check` passed. |
| 011 | The 100-loop ledger itself had no automated integrity check. As the loop count grows, progress text, row count, or unverified score text could drift. | Added `scripts/evaluate-docs.mjs` and the root `npm run docs:evaluate` script. The script checks ledger progress vs row count, rejects unverified rows, and scans docs for stale paste target aliases plus required task/JSONPath guidance. | Runtime health: 5 -> 5 for loop accounting. Evidence: `scripts/evaluate-docs.mjs`; `package.json`; `npm run docs:evaluate` passed. |
| 012 | The docs evaluation command was separate from the main verification path, so release verification could still pass while the loop ledger or docs consistency failed. | Added `npm run docs:evaluate` to the root `verify` script after package smoke. | Runtime health: 5 -> 5 for release gating. Evidence: `package.json`; `npm run docs:evaluate` passed. |
| 013 | The site doc was authored as Markdown, but the workbench Markdown viewer flattened tables into pipe-text paragraphs. The task-entrypoint table existed in source but did not read like a doc page in the demo. | Added Markdown table parsing and rendering to the workbench viewer and guarded table rendering in the workbench test. | Task discoverability: 5 -> 5 with stronger demo evidence. Evidence: `apps/site/src/playgrounds/InterfaceWorkbench.playground.tsx`; `apps/site/tests/interface-workbench.test.tsx`; `npm test -w @zod-crud/site` passed. |
| 014 | The demo's `doc.can*` group did not expose the whole public capability family. `canPatch`, `canRemove`, `canMove`, `canDuplicate`, `canCut`, and `canPaste` were missing from runnable UI even though docs listed them. | Added buttons for all missing capability methods and result snippets that show the exact public calls. | Task discoverability: 5 -> 5 for capability checks. API correctness evidence: stronger. Evidence: `apps/site/src/playgrounds/InterfaceWorkbench.playground.tsx`; `apps/site/tests/interface-workbench.test.tsx`; `npm test -w @zod-crud/site` passed. |
| 015 | A few workbench result labels still used internal shorthand such as `patch.remove` and `query.select`, which weakened the demo as a copyable external-interface reference. | Replaced those labels with public-call snippets, including `doc.patch(selectedPointers.map(...))` and `doc.query(jsonPath); doc.selection?.selectRanges(pointers)`. | LLM copyability: 5 -> 5 with less ambiguous result output. Evidence: `apps/site/src/playgrounds/InterfaceWorkbench.playground.tsx`; `npm test -w @zod-crud/site` passed. |
| 016 | The human public export lists did not match the root export surface. Important exported types such as `JSONDocumentChangeListener`, `JSONDocumentCommitOptions`, `JSONDocumentCommitSelection`, and `JSONDocumentLoadOptions` were missing from docs. | Completed the README and site public export snippets and added docs consistency coverage for every documented root export name across README, SPEC, and site docs. | API correctness evidence: 5 -> 5 for public export completeness. Evidence: `packages/zod-crud/README.md`; `apps/site/src/docs/zod-crud-api.md`; `apps/site/tests/docs-consistency.test.ts`; `npm test -w @zod-crud/site` passed. |
| 017 | SPEC's top-level concept map omitted `duplicate(pointer, options)`, even though duplicate is a first-class public document action in README, site docs, and `llms.txt`. | Added `duplicate(pointer, options)` to the SPEC concept map and made `docs:evaluate` require it. | API correctness evidence: stronger for the public concept map. Evidence: `packages/zod-crud/SPEC.md`; `scripts/evaluate-docs.mjs`; `npm run docs:evaluate` passed. |
| 018 | Release verification guidance was split: root `verify` now ran docs evaluation, but README, site docs, SPEC, and `llms.txt` did not consistently say that docs drift and the loop ledger are release-gated. | Added verification guidance with `docs:evaluate` to README, site docs, SPEC, and `llms.txt`, then added docs consistency and evaluator checks. | Runtime health: 5 -> 5 for release documentation. Evidence: `packages/zod-crud/README.md`; `apps/site/src/docs/zod-crud-api.md`; `packages/zod-crud/SPEC.md`; `llms.txt`; `apps/site/tests/docs-consistency.test.ts`; `scripts/evaluate-docs.mjs`; `npm test -w @zod-crud/site` and `npm run docs:evaluate` passed. |
| 019 | The package smoke test had a useful compile-time rejection for legacy `{ at }` paste targets, but the docs evaluator did not require that negative guard to stay in place. | Extended `docs:evaluate` to check the package smoke script for the `@ts-expect-error` guard proving `{ at }` is intentionally not a public paste target. | Runtime health: 5 -> 5 for legacy-alias regression coverage. Evidence: `scripts/evaluate-docs.mjs`; `packages/zod-crud/test/package-smoke.mjs`; `npm run docs:evaluate` passed. |
| 020 | Selection docs still showed only a small multi-select example. They did not give a compact map of the actual selection read, movement, text-planning, serialization, and subscription surface. | Added selection surface tables to README and site docs and guarded `selectedPointers`, `textPatch`, and `restore(snapshot)` in docs consistency and `docs:evaluate`. | State observability: 4 -> 5 for selection docs. Task discoverability evidence: stronger. Evidence: `packages/zod-crud/README.md`; `apps/site/src/docs/zod-crud-api.md`; `apps/site/tests/docs-consistency.test.ts`; `scripts/evaluate-docs.mjs`; `npm test -w @zod-crud/site` and `npm run docs:evaluate` passed. |
| 021 | History docs showed undo, redo, and transaction, but not enough detail about serializable metadata, `mergeKey`, explicit final selection, or `mergeLast`. | Added history metadata examples to README and site docs and guarded `mergeKey` plus `mergeLast` in docs consistency and `docs:evaluate`. | State observability: 5 -> 5 for history metadata. LLM copyability evidence: stronger. Evidence: `packages/zod-crud/README.md`; `apps/site/src/docs/zod-crud-api.md`; `apps/site/tests/docs-consistency.test.ts`; `scripts/evaluate-docs.mjs`; `npm test -w @zod-crud/site` and `npm run docs:evaluate` passed. |
| 022 | The package had many source-entry tests and a package smoke, but the smoke runtime path did not exercise a realistic installed-package document workflow across query, selection, clipboard, duplicate, can*, and history. | Extended `package-smoke.mjs` with an installed `zod-crud` public-interface scenario that imports from the package entrypoint and runs query-to-selection, clipboard spread paste, duplicate with rekey, `canCopy`, `canUndo`, and undo. Added an evaluator check that this scenario remains present. | Runtime health: 5 -> 5 for external-interface coverage. API correctness evidence: stronger. Evidence: `packages/zod-crud/test/package-smoke.mjs`; `scripts/evaluate-docs.mjs`; `npm run smoke:package -w zod-crud` and `npm run docs:evaluate` passed. |
| 023 | The workbench test clicked representative actions, but did not assert that each public facade group kept all expected controls visible. A future demo edit could silently drop a method from the interface map. | Added a control-discoverability test that checks the expected button list for `doc.patch`, document actions, selection, clipboard, history, query, `can*`, schema, and pure exports. | Runtime health: 5 -> 5 for demo interface coverage. Task discoverability evidence: stronger. Evidence: `apps/site/tests/interface-workbench.test.tsx`; `npm test -w @zod-crud/site` passed. |
| 024 | Markdown table rendering was guarded by the component test, but the release docs evaluator did not know that table rendering is part of the API docs demo contract. | Added `docs:evaluate` checks for workbench table block support, `<table>` rendering, and the table role assertion in the workbench test. | Runtime health: 5 -> 5 for docs demo release gating. Evidence: `scripts/evaluate-docs.mjs`; `npm run docs:evaluate` passed. |
| 025 | `JSONDocument<T>` exposes `canFind(jsonpath)`, but README, site docs, `llms.txt`, and the demo `can*` controls omitted it. That made the can* family non-MECE and hid query validation. | Added `canFind` to README, site docs, `llms.txt`, the workbench `doc.can*` group, component tests, docs consistency tests, and `docs:evaluate`. | API correctness: 5 -> 5 with a real public-interface omission fixed. Task discoverability evidence: stronger. Evidence: `packages/zod-crud/README.md`; `apps/site/src/docs/zod-crud-api.md`; `llms.txt`; `apps/site/src/playgrounds/InterfaceWorkbench.playground.tsx`; `apps/site/tests/docs-consistency.test.ts`; `apps/site/tests/interface-workbench.test.tsx`; `scripts/evaluate-docs.mjs`; `npm run typecheck -w @zod-crud/site`, `npm test -w @zod-crud/site`, and `npm run docs:evaluate` passed. |
| 026 | The final source layout changed the mental model, but release-facing docs did not yet share one SSOT for root entrypoints, internal concept layers, and the removed extra public layer plus React indirection. | Added final release notes, documented the source-layout SSOT across README, SPEC, site docs, `llms.txt`, and the API usage gap ledger, then made docs consistency and `docs:evaluate` reject stale source-layout paths. | API correctness evidence: stronger for import boundaries. Runtime health: 5 -> 5 for source-layout drift. Evidence: `docs/release-notes.md`; `packages/zod-crud/README.md`; `packages/zod-crud/SPEC.md`; `apps/site/src/docs/zod-crud-concepts.md`; `apps/site/src/docs/zod-crud-api.md`; `llms.txt`; `docs/api-usage-gaps.md`; `apps/site/tests/docs-consistency.test.ts`; `scripts/evaluate-docs.mjs`; `npm test -w @zod-crud/site`, `npm run docs:evaluate`, and `npm run verify` passed. |
| 027 | Large core editing workloads had no repeatable local measurement, and history undo/redo revalidated the whole Zod schema even though entries were generated from already accepted patches. That made large batch replay slower than necessary and left future performance work without a baseline. | Added `scripts/benchmark-core.mjs` plus `npm run perf:core`; added an internal trusted history replay path that keeps patch atomicity but skips schema revalidation for undo/redo; added a regression test proving undo/redo does not re-run whole-schema validation. | Performance regression: 0 -> 2. Runtime health evidence: stronger for history replay. Evidence: `scripts/benchmark-core.mjs`; `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/src/application/document/createJSON.ts`; `packages/zod-crud/src/application/document/createJSONDocument.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`, targeted history tests, and `npm run perf:core` passed. Current benchmark snapshot: 10k item undo/redo batch 1000 around 3ms; 50k item undo/redo batch 1000 around 75-80ms; individual 100 patches on 50k items around 11s. |
| 028 | Batch history recording still recomputed inverses by replaying every operation through the raw patch mutator. For common burst edits made of independent `replace` operations, that duplicated structural-copy work even though each inverse only needs the pre-edit value at the same pointer. | Added a fast inverse path for independent non-root `replace` batches, using sorted JSON Pointer strings to reject duplicate or parent/child overlap in `O(n log n)` before reading previous values. Expanded the core benchmark to report `history=0` and `history=100` batch timings separately, and added a regression test for repeated and nested paths so unsafe batches still use the general path. | Performance regression: 2 -> 3 for batch history recording. Evidence: `packages/zod-crud/src/foundation/json-patch/inverse.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`, `npm test -w zod-crud -- document-history-performance`, and `npm run perf:core` passed. |
| 029 | Independent `replace` batches still applied each operation through the raw mutator, so editing 1000 leaves under one large array copied the same ancestor array repeatedly. The benchmark showed this as the remaining large batch cost after history recording was optimized. | Added a fast apply path for independent non-root `replace` batches. It validates that all target paths exist, rejects duplicate or parent/child overlap, builds a replacement tree, and copies each touched container once before the usual JSON-serializable and schema gates. The same fast path is also used by trusted history replay. Added RFC 6902 tests for applied order, structural sharing, repeated/nested fallback semantics, and atomic rejection of non-serializable replacement values. | Performance regression: 3 -> 4 for burst replace batches. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/test/rfc6902.test.ts`; `npm run typecheck -w zod-crud`, targeted RFC/history/structural-sharing tests, and `npm run perf:core` passed. Current benchmark snapshot: 50k item batch 1000 around 145ms with history, and undo/redo batch 1000 around 1ms. |
| 030 | The new replace-batch fast path needed stricter proof that it preserves RFC array index validation, and `applyPatch` still performed a full JSON-serializability traversal after applying operations even though the input state and every operation value had already passed JSON checks. | Switched fast path target validation to the same strict array-index read semantics used by the raw mutator, added a regression test for `/items/01/...`, removed the redundant final JSON traversal after successful patch application, and split individual patch benchmark timings into `history=0` and `history=N`. | Performance regression: 4 -> 4 with stronger correctness evidence and lower traversal overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/src/foundation/json-patch/inverse.ts`; `packages/zod-crud/test/rfc6902.test.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`, targeted RFC/history/conformance tests, and `npm run perf:core` passed. Current benchmark snapshot: 50k item batch 1000 around 58-72ms; 50k individual 100 patch calls around 6.6-6.8s. |
| 031 | The docs still showed `history.transaction(() => doc.patch(); doc.patch())` as the main way to group synchronous edits. For large documents that is a performance trap because transaction merges history entries but each `doc.patch(...)` still runs a schema validation pass. | Replaced the history examples in README, site docs, and SPEC with `doc.commit([...], { label })` for known burst edits; added explicit transaction guidance to README, site docs, SPEC, and `llms.txt`; made docs consistency and `docs:evaluate` require the warning that transaction is not a validation batch. | Performance regression: 4 -> 4 with stronger usage guidance for the remaining individual-patch bottleneck. Evidence: `packages/zod-crud/README.md`; `apps/site/src/docs/zod-crud-api.md`; `packages/zod-crud/SPEC.md`; `llms.txt`; `apps/site/tests/docs-consistency.test.ts`; `scripts/evaluate-docs.mjs`; `npm test -w @zod-crud/site -- docs-consistency` and `npm run docs:evaluate` passed. |
| 032 | `doc.commit(..., { selection })` predicted the next document with `applyPatch`, then applied the same patch again through the state owner. Collapsed selection restore also expanded ranges by walking the whole document, making common text/caret commits much slower on large JSON. | Added an internal trusted state adoption path so selection commits reuse the already-validated predicted state and applied patch records; made collapsed range expansion return immediately without document traversal; added regression tests for single validation and no collapsed-range sibling walk; added a `doc.commit single leaf + selection` benchmark. | Performance regression: 4 -> 5 for selection commits. Evidence: `packages/zod-crud/src/application/document/createJSON.ts`; `packages/zod-crud/src/application/document/createJSONDocument.ts`; `packages/zod-crud/src/domain/selection/range.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `packages/zod-crud/tests/selection-headless.test.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`, targeted selection/history tests, and `npm run perf:core` passed. Current benchmark snapshot: 50k `doc.commit` single leaf + selection around 54ms, matching ordinary single-leaf patch. |
| 033 | Normal `doc.patch` still paid the public `applyPatch` boundary cost by scanning the entire current state for JSON serializability on every edit, even though the document owner can know whether its current state is trusted JSON. On large documents this dominated short single-leaf edits and repeated individual edits. | Split `applyPatch` into a public boundary wrapper plus an internal schema-validating trusted-state path; taught `createJSON` to track whether current state is JSON-trusted and use the trusted path only in that case; routed selection commit prediction through the same owner preview path; added regression tests that public `applyPatch` still rejects non-serializable input state, document patches still reject non-serializable op values, and untrusted schema output still keeps the public guard. | Performance regression: 5 -> 5 with lower document edit baseline and stronger JSON-boundary proof. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/src/application/document/createJSON.ts`; `packages/zod-crud/src/application/document/createJSONDocument.ts`; `packages/zod-crud/test/rfc6902.test.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`, targeted RFC/document tests, and `npm run perf:core` passed. Current benchmark snapshot: 50k public `applyPatch` single leaf around 74ms; 50k `doc.patch` single leaf + history around 4ms; 50k `doc.commit` single leaf + selection around 3.5ms; 50k batch 1000 around 5ms; undo/redo batch 1000 under 1ms. |
| 034 | Document capability checks such as `doc.canPatch` and pointer-based `doc.canReplace` still used the public preflight path, so a UI that checks capability before applying a short edit could pay the same large whole-state JSON boundary cost that `doc.patch` had just avoided. | Added a reusable `preFlightFromApplyResult` adapter, let document checks use the state owner's trusted preview path when available, and kept the public preflight fallback for non-document callers. Extended the core benchmark with `doc.canPatch single leaf` and added a regression test proving untrusted schema output still returns `not_serializable` instead of bypassing the public JSON guard. | Performance regression: 5 -> 5 with capability checks aligned to document edit cost. Evidence: `packages/zod-crud/src/domain/schema/preFlight.ts`; `packages/zod-crud/src/application/document/check.ts`; `packages/zod-crud/src/application/document/createJSONDocument.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`, targeted document/check tests, and `npm run perf:core` passed. Current benchmark snapshot: 50k `doc.canPatch` single leaf around 3.5ms, matching 50k `doc.patch` single leaf + history around 4.9ms instead of the public `applyPatch` boundary around 58ms. |
| 035 | Repeated individual `doc.patch` calls were still expensive because every accepted patch re-ran whole-document Zod validation. Batch edits were fast, but a short burst expressed as 100 individual leaf replacements on a 50k-item plain schema still cost hundreds of milliseconds. | Added a conservative local schema validation path for trusted document state: only independent non-root `replace` operations on plain structural Zod schemas skip root validation, and any schema checks/refinements/transforms fall back to the full validation path. Added regression tests proving plain structural leaf edits do not call root `safeParse`, invalid leaf values are still rejected atomically, and cross-field `superRefine` still runs and rejects invalid edits. | Performance regression: 5 -> 5 with the main individual-patch bottleneck reduced for plain schemas. Evidence: `packages/zod-crud/src/domain/schema/localPatch.ts`; `packages/zod-crud/src/application/document/createJSON.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`, targeted performance/RFC/document tests, and `npm run perf:core` passed. Current benchmark snapshot: 50k `doc.patch` single leaf + history around 0.09ms; 50k `doc.canPatch` single leaf around 0.09ms; 50k individual `doc.patch` 100 calls around 5-7ms; 50k batch 1000 around 1.5-2ms; public `applyPatch` still around 58ms because it retains the external JSON boundary. |
| 036 | The local validation optimization covered replace bursts, but array-heavy editing workflows also use append, remove, copy, and move. Those operations were not measured separately, and unsupported object-key mutations still needed explicit proof that they fall back to whole-schema validation. | Extended local validation to single array `add`, `remove`, `copy`, and `move` operations on plain structural schemas only. Target array values are checked against the array element schema, source array removals are restricted to array parents, and object-key removals still fall back to full validation. Added benchmark rows for individual array add/remove bursts and regression tests for local array mutation, invalid array inserts, and required object-key removal fallback. | Performance regression: 5 -> 5 with array mutation bursts covered. Evidence: `packages/zod-crud/src/domain/schema/localPatch.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`, targeted performance/RFC/document tests, and `npm run perf:core` passed. Current benchmark snapshot: 50k individual replace 100 calls around 5ms; 50k individual add 100 calls around 25ms; 50k individual remove 100 calls around 15ms; 50k batch 1000 around 2ms; public `applyPatch` single leaf remains around 61ms at the external boundary. |
| 037 | Mixed-operation batches still fell back to whole-document validation unless every operation was an independent replace. That meant a patch containing several array appends/removes plus a leaf replace could lose the local validation benefit even on plain structural schemas. | Generalized the local validation path to sequentially accept mixed `replace`, array `add`, array `remove`, array `copy`, and array `move` operations on plain structural schemas. Each step validates only the affected value or array element and rolls the whole patch back on the first local schema violation. Added a mixed batch regression test that includes add/copy/move/remove/replace, proves root `safeParse` is not called, and proves later invalid operations keep the document unchanged. Added a mixed array batch benchmark row. | Performance regression: 5 -> 5 with mixed plain-schema patches covered. Evidence: `packages/zod-crud/src/domain/schema/localPatch.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`, targeted performance/RFC/document tests, and `npm run perf:core` passed. Current benchmark snapshot: 50k mixed array batch 100 around 26ms; 50k individual add 100 around 24ms; 50k individual remove 100 around 14ms; 50k replace batch 1000 around 2ms; public `applyPatch` single leaf around 54ms at the external boundary. |
| 038 | The mixed array batch local path still applied each operation through the generic patch engine, so a same-array add/remove batch copied the large array once per operation. The benchmark exposed this as the remaining cost for array-heavy bursts even after root schema validation was removed. | Added a same-array `add`/`remove` local fast path that validates all added elements, applies RFC-ordered insert/remove operations to one array copy, and writes the parent array back with trusted structural sharing instead of re-running JSON serializability over the whole array. Kept unsupported/malformed operations on the generic fallback path and added a regression test for non-serializable same-array add rollback. | Performance regression: 5 -> 5 with same-array mutation batches copying the array once. Evidence: `packages/zod-crud/src/domain/schema/localPatch.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`, targeted performance/RFC/document tests, and `npm run perf:core` passed. Current benchmark snapshot: 50k mixed array batch 100 around 2.8ms, down from about 26ms; 50k individual add 100 around 25ms; 50k individual remove 100 around 14ms; 50k replace batch 1000 around 2ms; public `applyPatch` single leaf around 56ms at the external boundary. |
| 039 | History-enabled same-array add/remove batches still had two hidden costs: inverse calculation replayed each operation through the generic raw patch mutator, and undo/redo replay also copied the large array once per operation. This could make history noticeably slower than the now-fast no-history mixed array batch path. | Added same-array `add`/`remove` fast paths to foundation patch replay and inverse calculation. Inverses now simulate only the affected array to compute concrete append indexes and removed values, while trusted replay applies same-array add/remove batches with one array copy. Extended the benchmark with mixed array `history=100`, undo, and redo rows, and added a regression test proving same-array add/remove history restores without schema revalidation. | Performance regression: 5 -> 5 with history costs aligned to batch apply. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/src/foundation/json-patch/inverse.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`, targeted performance/RFC/document tests, and `npm run perf:core` passed. Current benchmark snapshot: 50k mixed array batch 100 history=0 around 2.7ms; history=100 around 3.0ms; undo around 2.1ms; redo around 2.5ms; 50k replace batch 1000 history=100 around 2.1ms. |
| 040 | The optimized core paths were measurable, but public docs did not say when fast local validation applies, when full root validation intentionally remains, or why public `applyPatch` can still be slower on huge states. That could lead users to benchmark the wrong surface or misuse `history.transaction` as a validation batch. | Added compact performance guidance to README, SPEC, site API docs, and `llms.txt`: document facade hot paths, trusted document state, plain structural Zod schema limits, supported fast-path operations, full root schema validation fallback, and `npm run perf:core`. Added docs consistency and `docs:evaluate` checks so those boundaries stay documented. | Performance regression evidence: stronger user-facing measurement and usage guidance. LLM copyability evidence: stronger for large-edit workflows. Evidence: `packages/zod-crud/README.md`; `packages/zod-crud/SPEC.md`; `apps/site/src/docs/zod-crud-api.md`; `llms.txt`; `apps/site/tests/docs-consistency.test.ts`; `scripts/evaluate-docs.mjs`; `npm test -w @zod-crud/site -- docs-consistency`, `npm run docs:evaluate`, `npm run typecheck -w @zod-crud/site`, `git diff --check`, and `npm run perf:core` passed. Current benchmark snapshot: 50k `doc.patch` single leaf around 0.09ms; 50k `doc.canPatch` single leaf around 0.08ms; 50k replace batch 1000 history=100 around 2.3ms; mixed array batch 100 history=100 around 2.8ms; undo/redo mixed array batch around 2.1/2.5ms; public `applyPatch` single leaf around 54ms at the external boundary. |
| 041 | Same-array batch fast paths covered add/remove, but reorder and duplicate-heavy list editing still used generic copy/move replay. A burst with many same-array `copy` and `move` operations could copy the large array once per operation and history replay had no targeted measurement for that workload. | Generalized the same-array fast path from add/remove to add/remove/copy/move in foundation replay, local plain-schema validation, and inverse calculation. Copy uses a trusted JSON clone only on the trusted fast path. Added RFC ordering and structural-sharing coverage, local validation coverage, history restore coverage, and benchmark rows comparing individual copy/move calls to batched copy/move plus undo/redo. | Performance regression: 5 -> 5 with reorder/duplicate bursts covered. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/src/foundation/json-patch/inverse.ts`; `packages/zod-crud/src/domain/schema/localPatch.ts`; `packages/zod-crud/src/foundation/json.ts`; `packages/zod-crud/test/rfc6902.test.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`, `npm test -w zod-crud -- rfc6902 document-history-performance`, `npm test -w zod-crud`, `npm run smoke:package -w zod-crud`, `npm run docs:evaluate`, `git diff --check`, and `npm run perf:core` passed. Current benchmark snapshot: 50k individual copy/move 100 around 43ms; 50k copy/move batch 100 history=0 around 8.0ms; history=100 around 9.2ms; undo/redo copy/move batch around 3.8/7.8ms. |
| 042 | Local validation still rediscovered plain-structural schema status and pointer-to-schema mappings on every patch. That overhead is small per edit but repeats across hot `doc.patch`, `doc.canPatch`, and large replace batches, especially when many operations target the same shape. | Added WeakMap-backed caches for plain structural schema classification and local pointer schema lookup. Kept the cache scoped to schema objects, left runtime `safeParse` behavior unchanged, and added same-array copy object-safety coverage for `__proto__` data keys while tightening invalid same-array move handling to fall back instead of accepting impossible append indexes. | Performance regression: 5 -> 5 with lower repeated local-validation overhead. Evidence: `packages/zod-crud/src/domain/schema/localPatch.ts`; `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/src/foundation/json-patch/inverse.ts`; `packages/zod-crud/test/rfc6902.test.ts`; `npm run typecheck -w zod-crud`, `npm test -w zod-crud -- rfc6902 document-history-performance`, `npm test -w zod-crud`, `npm run smoke:package -w zod-crud`, `git diff --check`, and `npm run perf:core` passed. Current benchmark snapshot: 50k replace batch 1000 history=0 around 1.5ms; history=100 around 2.2ms; 50k `doc.patch` single leaf around 0.13ms; 50k mixed array batch 100 history=0 around 2.7ms; 50k copy/move batch 100 history=0 around 8.1ms. |
| 043 | Public `applyPatch` still spent most of its large-state time in the external JSON boundary. The guard walked object keys twice and allocated a new path array for each child, which made a valid 50k-item state expensive even before the actual patch work. | Reworked `jsonSerializableError` to traverse descriptors and values in one pass and reuse a mutable path stack for error pointers instead of allocating child path arrays. Added serializability regressions for non-enumerable object fields, accessor object fields, sparse arrays, non-enumerable array indexes, accessor array indexes, and extra array properties so the faster traversal keeps the JSON boundary contract. | Performance regression: 5 -> 5 with lower public boundary overhead. Evidence: `packages/zod-crud/src/foundation/json.ts`; `packages/zod-crud/test/rfc6902.test.ts`; `npm run typecheck -w zod-crud`, `npm test -w zod-crud -- rfc6902 document-history-performance`, `npm test -w zod-crud`, `npm run smoke:package -w zod-crud`, `git diff --check`, and `npm run perf:core` passed. Current benchmark snapshot: 50k public `applyPatch` single leaf around 45ms; 10k public `applyPatch` single leaf around 8.9ms; 50k `doc.patch` single leaf around 0.09ms; 50k replace batch 1000 history=100 around 1.9ms; 50k mixed array batch 100 history=0 around 2.6ms. |
| 044 | Clipboard copy, cut, write, and read cloned JSON payloads with `cloneJson` even after the same payload had already passed `jsonSerializableError`. For large selected subtrees this repeated the external JSON boundary traversal before the actual clone. The benchmark also did not measure large clipboard payloads. | Switched verified clipboard payload paths to `cloneTrustedJson`: domain copy/cut after validation, clipboard write after validation, and clipboard read from an already trusted buffer. Added a regression test that `clipboard.read()` still returns an isolated clone, and extended the core benchmark with whole-`/items` clipboard copy/write/read rows. | Performance regression evidence: stronger for large clipboard workflows and lower duplicate validation cost. Evidence: `packages/zod-crud/src/domain/verbs/copy.ts`; `packages/zod-crud/src/domain/verbs/cut.ts`; `packages/zod-crud/src/application/document/clipboard.ts`; `packages/zod-crud/tests/create-json-document.test.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`, targeted clipboard tests, `npm test -w zod-crud`, `npm run smoke:package -w zod-crud`, `git diff --check`, and `npm run perf:core` passed. Current benchmark snapshot: 50k `doc.clipboard.copy("/items")` around 66ms; 50k `doc.clipboard.write(items)` around 66ms; 50k `doc.clipboard.read()` around 26ms; 50k `doc.patch` single leaf around 0.08ms; 50k public `applyPatch` single leaf around 45ms. |
| 045 | After duplicate validation was removed from clipboard paths, large payload clone cost became visible. `cloneTrustedJson` still used `JSON.stringify`/`JSON.parse`, which was much slower than a JSON-value-specific clone for the already validated object/array graph. | Replaced trusted cloning with a direct recursive JSON clone. It preserves arrays and plain objects, returns primitives as-is, and defines `__proto__` as an own enumerable data property so object-safety behavior stays aligned with JSON parsing. `cloneJson` now validates once and delegates to the same trusted clone. | Performance regression: 5 -> 5 with lower large clone cost. Evidence: `packages/zod-crud/src/foundation/json.ts`; existing object-safety and clipboard clone-isolation tests; `npm run typecheck -w zod-crud`, targeted JSON/clipboard/history tests, `npm test -w zod-crud`, `npm run smoke:package -w zod-crud`, `git diff --check`, and `npm run perf:core` passed. Current benchmark snapshot: 50k `doc.clipboard.read()` around 7.2ms, down from about 26ms; 50k `doc.clipboard.write(items)` around 52ms; 10k clipboard read around 1.4ms; 50k `doc.patch` single leaf around 0.09ms. |
| 046 | Document clipboard copy/cut still treated selected document values like external payloads. Even when the document owner already knew current state was trusted JSON, copying a huge selected subtree re-ran serializability validation before cloning it. | Exposed the internal `stateJsonTrusted` signal from the JSON state owner to document clipboard operations, and added trusted options to pure `copy`/`cut` so document-owned JSON can skip the duplicate subtree validation. Kept external `clipboard.write` validation unchanged, and added regressions proving untrusted schema output still rejects clipboard copy/cut with `not_serializable`. | Performance regression: 5 -> 5 with large document clipboard copy aligned to clone cost. Evidence: `packages/zod-crud/src/application/document/createJSON.ts`; `packages/zod-crud/src/application/document/createJSONDocument.ts`; `packages/zod-crud/src/application/document/clipboard.ts`; `packages/zod-crud/src/domain/verbs/copy.ts`; `packages/zod-crud/src/domain/verbs/cut.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`, targeted clipboard/RFC tests, `git diff --check`, and `npm run perf:core` passed. Current benchmark snapshot: 50k `doc.clipboard.copy("/items")` around 12.4ms, down from about 66ms; 50k `doc.clipboard.write(items)` around 46.4ms; 50k `doc.clipboard.read()` around 7.0ms; 50k `doc.patch` single leaf around 0.07ms; 50k public `applyPatch` single leaf around 46ms. |
| 047 | After document clipboard copy became trusted-state aware, capability checks still used the slower pure clipboard verbs as if they were external payload operations. A toolbar or command palette checking `doc.canCopy("/items")` or `doc.canCut(...)` on a large selection could therefore pay tens of milliseconds before the real action. | Passed the document trusted-state signal into `buildCheck`, let `canCopy` avoid cloning payloads it immediately discards, and let `canCut`/`clipboard.cut` use the document owner's trusted preview path instead of public preflight when available. Added benchmark rows for large `canCopy` and `canCut`, and regression tests proving untrusted schema output still fails plus plain structural cut checks do not rerun root schema parse. | Performance regression: 5 -> 5 with capability checks aligned to document hot paths. Evidence: `packages/zod-crud/src/application/document/check.ts`; `packages/zod-crud/src/application/document/clipboard.ts`; `packages/zod-crud/src/application/document/createJSONDocument.ts`; `packages/zod-crud/src/domain/verbs/copy.ts`; `packages/zod-crud/src/domain/verbs/cut.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`, targeted document tests, `npm test -w zod-crud`, `npm run smoke:package -w zod-crud`, `npm run docs:evaluate`, `git diff --check`, and `npm run perf:core` passed. Current benchmark snapshot: 50k `doc.canCopy("/items")` around 0.02ms, down from about 37ms; 50k `doc.canCut` last item around 0.16ms, down from about 36ms; 50k `doc.clipboard.copy("/items")` around 8.4ms; 50k `doc.clipboard.write(items)` around 39.7ms; 50k public `applyPatch` single leaf around 46.8ms. |
| 048 | Paste and paste capability checks still used pure `paste` with public preflight. That meant appending one small item through `doc.canPastePayload`, `doc.clipboard.pastePayload`, or clipboard-backed `canPaste` could rescan a 50k-item document before the document owner applied the same patch through its trusted path. | Added a trusted preview option to the paste verb and routed document `canPastePayload`, `canPaste`, `clipboard.paste`, and `clipboard.pastePayload` through the state owner's preview path. Added benchmark rows for paste capability and paste mutation, plus regressions proving plain structural paste checks skip root schema parse while untrusted schema output still fails with `not_serializable`. | Performance regression: 5 -> 5 with paste checks aligned to document hot paths. Evidence: `packages/zod-crud/src/domain/verbs/paste.ts`; `packages/zod-crud/src/application/document/check.ts`; `packages/zod-crud/src/application/document/clipboard.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`, targeted document tests, `npm test -w zod-crud`, `npm run smoke:package -w zod-crud`, `npm run docs:evaluate`, `git diff --check`, and `npm run perf:core` passed. Current benchmark snapshot: 50k `doc.canPastePayload` single append around 0.23ms, down from about 34ms; 50k `doc.clipboard.pastePayload` single append around 0.44ms, down from about 35ms; 50k `doc.canCopy("/items")` around 0.02ms; 50k `doc.patch` single leaf around 0.08ms; 50k public `applyPatch` single leaf around 42ms. |
| 049 | Higher-level document actions still had hidden public-boundary costs. `doc.canDuplicate`, `doc.duplicate`, `doc.canMove`, and JSONPath `doc.canReplace` composed pure verbs that called public preflight directly, so a single duplicate/move/check in a 50k-item document could cost 40-50ms even though the equivalent document patch hot path was sub-millisecond. | Added internal trusted preview options to duplicate, move, and JSONPath replace verbs. Routed document capability checks through those previews, and changed `doc.duplicate` to adopt the already predicted next state with `trustedApply` while still recording history and selection metadata. Added benchmark rows and regressions proving plain structural actions avoid root schema parse while untrusted schema output still keeps the JSON guard. | Performance regression: 5 -> 5 with higher-level actions aligned to document hot paths. Evidence: `packages/zod-crud/src/domain/verbs/duplicate.ts`; `packages/zod-crud/src/domain/verbs/move.ts`; `packages/zod-crud/src/domain/verbs/replace.ts`; `packages/zod-crud/src/application/document/check.ts`; `packages/zod-crud/src/application/document/createJSONDocument.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`, targeted document/RFC tests, `npm test -w zod-crud`, `npm run smoke:package -w zod-crud`, `npm run docs:evaluate`, `git diff --check`, and `npm run perf:core` passed. Current benchmark snapshot: 50k `doc.canDuplicate` middle around 0.30ms, down from about 43ms; 50k `doc.duplicate` middle around 0.27ms, down from about 51ms; 50k `doc.canMove` adjacent around 0.51ms, down from about 46ms; 50k JSONPath `doc.canReplace` one match around 0.12ms, down from about 47ms. |
| 050 | External clipboard writes still validated a large JSON payload and then cloned the same graph in a second traversal. Unlike document-owned copy/cut/paste, this boundary cannot skip validation, but it can avoid walking valid payloads twice. | Added `cloneJsonSerializable`, a one-pass JSON boundary that validates while producing the isolated clone. `cloneJson` now uses that helper, and `clipboard.write` stores the validated clone directly. Added regression tests proving external writes still clone defensively, preserve own `__proto__` data keys without prototype pollution, and reject accessor/sparse non-JSON payload shapes. | Performance regression: 5 -> 5 with lower external clipboard write boundary overhead. Evidence: `packages/zod-crud/src/foundation/json.ts`; `packages/zod-crud/src/application/document/clipboard.ts`; `packages/zod-crud/tests/create-json-document.test.ts`; existing RFC serializability tests; `npm run typecheck -w zod-crud`, targeted JSON/clipboard/history tests, `npm test -w zod-crud`, `npm run smoke:package -w zod-crud`, `npm run docs:evaluate`, `git diff --check`, and `PERF_ROUNDS=9 npm run perf:core` passed. Current benchmark snapshot: 50k `doc.clipboard.write(items)` around 41.4ms, down from the recent 49-50ms range; direct boundary comparison on the same payload showed old validate+clone around 42.2ms vs one-pass clone around 38.6ms; 50k `doc.clipboard.copy("/items")` around 10.9ms; 50k `doc.clipboard.read()` around 9.9ms. |
| 051 | After copy/read paths were reduced to trusted cloning, `cloneTrustedJson` still allocated a key array for every object with `Object.keys`. Large clipboard payloads contain many small objects, so the allocation overhead showed up directly in `doc.clipboard.copy("/items")`, `read()`, same-array copy/move replay, and any already-validated clone. | Switched trusted object cloning from `Object.keys` to `for...in` with an own-property guard, preserving the special own `__proto__` data-key handling while avoiding per-object key-array allocation on normal trusted JSON objects. Existing clone isolation, object-safety, and RFC serializability tests cover the behavior. | Performance regression: 5 -> 5 with lower trusted clone overhead. Evidence: `packages/zod-crud/src/foundation/json.ts`; existing clipboard clone and `__proto__` object-safety tests; `npm run typecheck -w zod-crud`, targeted JSON/clipboard/history tests, `npm test -w zod-crud`, `npm run smoke:package -w zod-crud`, `npm run docs:evaluate`, `git diff --check`, and `PERF_ROUNDS=9 npm run perf:core` passed. Current benchmark snapshot: 50k `doc.clipboard.copy("/items")` around 6.1ms, down from about 10.9ms; 50k `doc.clipboard.read()` around 7.3ms, down from about 9.9ms; local clone comparison on the same payload showed the `for...in` clone around 5.8ms vs the previous `Object.keys` clone around 11.4ms. |
| 052 | Rekeyed duplicate/paste still needed to scan existing values across the document. The scan used a generic `walk` that invoked callbacks for primitive leaves and allocated `Object.values()` arrays for every object, which showed up when duplicating one item with `rekey.fields` in a 50k-item document. | Changed the rekey walker to visit records only, skip primitive leaves, iterate arrays by index, and traverse object children with `for...in` plus an own-property guard instead of `Object.values()`. Added a benchmark row for `doc.duplicate` with rekey so this remains visible. | Performance regression: 5 -> 5 with lower rekey scan overhead. Evidence: `packages/zod-crud/src/domain/schema/rekey.ts`; `scripts/benchmark-core.mjs`; existing duplicate rekey and package-smoke rekey coverage; `npm run typecheck -w zod-crud`, targeted document/RFC tests, `npm test -w zod-crud`, `npm run smoke:package -w zod-crud`, `npm run docs:evaluate`, `git diff --check`, and `PERF_ROUNDS=9 npm run perf:core` passed. Current benchmark snapshot: 50k `doc.duplicate` middle with `rekey.fields=["id"]` around 6.0ms, down from about 8.5-9.0ms; non-rekey duplicate remains around 0.25ms. |
| 053 | The rekey scan was faster, but each visited record still iterated a `Set` of field names and looked up the matching existing-value set through a `Map`. Single-field rekey is the common duplicate/paste path, and document-owned duplicate payloads were still cloned through the external JSON boundary even though the source value was already trusted. | Replaced the rekey field lookup with compact field entries, added a single-field fast path that removes the per-record inner field loop, and added an internal trusted-payload option used by document duplicate, `canDuplicate`, and clipboard-buffer paste while leaving external `pastePayload` validation intact. Added a `doc.canDuplicate` with rekey benchmark row. | Performance regression: 5 -> 5 with lower common rekey overhead and visible capability coverage. Evidence: `packages/zod-crud/src/domain/schema/rekey.ts`; `packages/zod-crud/src/domain/verbs/duplicate.ts`; `packages/zod-crud/src/domain/verbs/paste.ts`; `packages/zod-crud/src/application/document/check.ts`; `packages/zod-crud/src/application/document/createJSONDocument.ts`; `packages/zod-crud/src/application/document/clipboard.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`, targeted document/RFC tests, `npm test -w zod-crud`, `npm run smoke:package -w zod-crud`, `npm run docs:evaluate`, `git diff --check`, and `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core` passed. Current benchmark snapshot: 50k `doc.duplicate` middle with `rekey.fields=["id"]` around 5.15ms, down from the previous 6.6ms local baseline; 50k `doc.canDuplicate` with rekey around 5.93ms; non-rekey duplicate remains around 0.35ms. |
| 054 | Suffix rekey still populated the existing-value set with every scalar id in a large document, even though the suffix strategy only needs the current value and its generated `-copy` candidates. On a 50k-item document, this made single-card duplicate/capability checks spend milliseconds adding unrelated ids that can never affect the minted value. | For suffix strategy, cloned the payload first, collected the payload field base values, and filtered the state scan so each field set records only exact bases and matching `base-copy` candidate values. Added coverage that unrelated suffix-like values such as `a-copy-x` do not block `a-copy`, while real `a-copy` collisions still produce `a-copy-2`. | Performance regression: 5 -> 5 with lower suffix rekey scan overhead. Evidence: `packages/zod-crud/src/domain/schema/rekey.ts`; `packages/zod-crud/tests/create-json-document.test.ts`; existing rekey capability benchmark rows in `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`, targeted document tests, `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`, full package tests, package smoke, docs evaluation, and `git diff --check` passed. Current benchmark snapshot: 50k `doc.duplicate` middle with `rekey.fields=["id"]` around 2.9ms, down from about 5.5ms; 50k `doc.canDuplicate` with rekey around 3.5ms, down from about 5.5ms. |
| 055 | Public JSON boundaries still paid path-stack push/pop costs for every valid property even though valid payloads never need an error pointer. This affected public `applyPatch` state validation and external `clipboard.write` clone+validation on 50k-item payloads. | Added fast valid-path traversals for `jsonSerializableError` and `cloneJsonSerializable` that keep the same descriptor-based JSON safety checks but avoid building path context unless an error is found. Invalid values fall back to the existing detailed traversal, and RFC tests now assert exact reasons such as `/a: function is not JSON` and `/computed: accessor property is not JSON`. | Performance regression: 5 -> 5 with lower external JSON boundary overhead. Evidence: `packages/zod-crud/src/foundation/json.ts`; `packages/zod-crud/test/rfc6902.test.ts`; `npm run typecheck -w zod-crud`, targeted JSON/document tests, `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`, full package tests, package smoke, docs evaluation, and `git diff --check` passed. Current benchmark snapshot: 50k public `applyPatch` single leaf around 39.1ms, down from the recent 42.8ms baseline; 50k `doc.clipboard.write(items)` around 41.4ms, down from the recent 44.4ms baseline. |
| 056 | After suffix filtering, the common single-card rekey still used the generic suffix matcher: a one-element `Set`, a one-element prefix array, and repeated `String(...)` conversion for every visited record. That overhead is small per record but repeats across 50k records on the hot duplicate/capability path. | Added a single-field/single-base suffix scan that compares directly against `base`, `base-copy`, and `base-copy-` prefixes, introduced `scalarText` to avoid `String(...)` allocation for normal string ids, and cached the own-property guard used by the rekey walker. Existing suffix collision tests cover behavior. | Performance regression: 5 -> 5 with lower common rekey overhead. Evidence: `packages/zod-crud/src/domain/schema/rekey.ts`; existing `packages/zod-crud/tests/create-json-document.test.ts` suffix coverage; `npm run typecheck -w zod-crud`, targeted document/RFC tests, `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`, full package tests, package smoke, docs evaluation, and `git diff --check` passed. Current benchmark snapshot: 50k `doc.duplicate` middle with `rekey.fields=["id"]` around 2.36ms, down from about 2.9ms; 50k `doc.canDuplicate` with rekey around 2.66ms, down from about 3.5ms. |
| 057 | Single array mutations still used spread over `slice()` results for insertion and removal. That creates extra intermediate arrays and is especially wasteful for tail removal, which is common in burst edits and participates in copy/move replay. | Replaced array `add`/`remove` mutation with one `slice()` plus `splice()` for middle edits, a direct append path for end inserts, and a `slice(0, idx)` path for tail removal. Added RFC coverage proving retained elements and the original array are not mutated. | Performance regression: 5 -> 5 with lower single array mutation overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/internal.ts`; `packages/zod-crud/test/rfc6902.test.ts`; `npm run typecheck -w zod-crud`, targeted RFC/history tests, `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`, full package tests, package smoke, docs evaluation, and `git diff --check` passed. Current benchmark snapshot: 50k individual remove 100 around 5.0ms, down from the recent 25ms range; 50k individual copy/move 100 around 39ms, down from the recent 55ms range. |
| 058 | Same-array fast paths still required at least two ops, so single array add/remove/copy/move fell back to generic replay. That affected burst-style individual edits, adjacent move checks, and single-op history inverse replay after array mutation itself was optimized. | Allowed same-array structural apply, local validation, and inverse calculation to handle single-op arrays by only rejecting empty op lists. Added a regression proving single same-array history undo/redo restores through the local fast path without rerunning root schema validation. | Performance regression: 5 -> 5 with single array operations on the same-array fast path. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/src/domain/schema/localPatch.ts`; `packages/zod-crud/src/foundation/json-patch/inverse.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`, targeted RFC/history tests, `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`, full package tests, package smoke, docs evaluation, and `git diff --check` passed. Current benchmark snapshot: 50k individual add 100 around 23.8ms, down from about 35ms; 50k individual copy/move 100 around 24.8ms, down from about 39ms; 50k `doc.canMove` adjacent around 0.08ms, down from about 0.23ms. |
| 059 | Same-array structural replay still used `splice()` for append, tail remove, and adjacent moves. In large copy/move batches this meant repeated element shifting even when the operation could be represented as `push`, `pop`, or a two-slot swap after the one required array clone. | Specialized same-array apply and inverse simulation for append add/copy, tail remove, and adjacent move. Added RFC coverage for adjacent same-array moves in both directions, preserving remove-then-add ordering and input immutability. | Performance regression: 5 -> 5 with lower copy/move batch and history replay overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/src/foundation/json-patch/inverse.ts`; `packages/zod-crud/test/rfc6902.test.ts`; `npm run typecheck -w zod-crud`, targeted RFC/history tests, `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`, full package tests, package smoke, docs evaluation, and `git diff --check` passed. Current benchmark snapshot: 50k copy/move array batch 100 history=0 around 8.9ms, down from about 10.3ms; history=100 around 9.8ms, down from about 11.8ms; undo around 2.6ms, down from about 3.3ms; redo around 7.1ms, down from about 7.9ms. |
| 060 | Public JSON-boundary validation still used recursive calls for every nested value in already-valid payloads. On a 50k-item state, that made public `applyPatch` and external clipboard writes spend most of their time just proving the same plain JSON shape again. | Replaced the fast valid/invalid JSON validator with an explicit stack traversal while keeping the detailed error-path validator unchanged. Adjusted the clone fast path to keep symbol-key checks on the valid path without weakening accessor, sparse-array, or non-enumerable guards, and added symbol-key regressions for both public patch input state and clipboard writes. | Performance regression: 5 -> 5 with lower valid JSON-boundary traversal overhead. Evidence: `packages/zod-crud/src/foundation/json.ts`; `packages/zod-crud/test/rfc6902.test.ts`; `packages/zod-crud/tests/create-json-document.test.ts`; `npm run typecheck -w zod-crud`, targeted RFC/clipboard tests, `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`, full package tests, package smoke, docs evaluation, and `git diff --check` passed. Current benchmark snapshot: direct 50k validator comparison in one Node process showed the previous recursive fast path around 26.0ms vs current stack traversal around 24.5ms; 50k public `applyPatch` single leaf around 38.6ms; 50k `doc.clipboard.write(items)` around 39.5ms. |
| 061 | `cloneJsonSerializable` still used the one-pass validate-and-clone walker for every payload size. For tiny payloads that is fastest, but for large top-level arrays the optimized JSON validator plus the trusted clone was measurably faster than carrying clone allocation through the descriptor-heavy validation loop. | Added a large-array clone path that validates with the fast JSON boundary first, then uses `cloneTrustedJson` only after the payload is proven serializable. Kept small payloads on the one-pass path and added large external clipboard array clone-isolation coverage. | Performance regression: 5 -> 5 with lower large external array clone cost. Evidence: `packages/zod-crud/src/foundation/json.ts`; `packages/zod-crud/tests/create-json-document.test.ts`; `npm run typecheck -w zod-crud`, targeted clipboard/RFC tests, direct 50k clone comparison, `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`, full package tests, package smoke, docs evaluation, and `git diff --check` passed. Current benchmark snapshot: direct 50k array clone comparison in one Node process showed previous one-pass clone around 37.0ms, validate+trusted around 34.3ms, and current `cloneJsonSerializable` around 31.2ms; 50k `doc.clipboard.write(items)` around 41.7ms with a 36.1ms minimum. |
| 062 | The stack-based JSON fast validator still pushed primitive leaf values onto the work stack and then popped them only to accept them. Large document states have hundreds of thousands of primitive leaves, so this added avoidable work to public `applyPatch` and external boundary validation. | Changed the fast validator to inspect descriptor values in place: object children still go onto the stack, while valid primitive leaves are accepted immediately and invalid primitive leaves return the same fast error category. Detailed error reporting remains on the existing fallback path. | Performance regression: 5 -> 5 with lower primitive-heavy JSON validation overhead. Evidence: `packages/zod-crud/src/foundation/json.ts`; existing serializability regressions in `packages/zod-crud/test/rfc6902.test.ts` and `packages/zod-crud/tests/create-json-document.test.ts`; `npm run typecheck -w zod-crud`, targeted RFC/clipboard tests, direct 50k validator comparison, `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`, full package tests, package smoke, docs evaluation, and `git diff --check` passed. Current benchmark snapshot: direct 50k validator comparison in one Node process showed previous stack-all validation around 26.8ms vs current primitive-skip validation around 25.3ms; 50k public `applyPatch` single leaf around 35.8ms; 50k `doc.clipboard.write(items)` around 37.4ms. |
| 063 | Trusted JSON clone still recursed into every primitive leaf and looked up `Object.prototype.hasOwnProperty` inside every object loop. This showed up in document-owned clipboard copy/read, large external array writes after validation, and same-array copy replay. | Changed `cloneTrustedJson` to assign primitive children directly and recurse only into object children, and cached the own-property guard used by its object loop. Added a hidden extra-property array regression while keeping existing clone isolation and `__proto__` object-safety coverage. | Performance regression: 5 -> 5 with lower trusted clone overhead. Evidence: `packages/zod-crud/src/foundation/json.ts`; `packages/zod-crud/test/rfc6902.test.ts`; existing clipboard clone isolation tests; `npm run typecheck -w zod-crud`, targeted RFC/clipboard/history tests, direct 50k clone comparison, `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`, full package tests, package smoke, docs evaluation, and `git diff --check` passed. Current benchmark snapshot: direct 50k trusted clone comparison in one Node process showed previous recursive-primitive clone around 6.8ms vs current `cloneTrustedJson` around 5.5ms; 50k `doc.clipboard.copy("/items")` around 5.0ms; 50k `doc.clipboard.read()` around 4.9ms; 50k `doc.clipboard.write(items)` around 34.0ms. |
| 064 | Single same-array append and append-copy still cloned the large array with `slice()` before pushing one value. For burst edits expressed as many individual `doc.patch({ op: "add", path: "/items/-" })` calls, this kept append-heavy workflows much slower than equivalent replace/remove bursts. | Added single-op same-array fast exits for append add, append copy, and tail remove: append uses `concat([value])`, tail remove uses `slice(0, index)`, and batch operations still use the one-copy replay path. Added RFC coverage proving single append/remove patches report concrete applied paths, preserve structural sharing, and leave the original array untouched. | Performance regression: 5 -> 5 with individual append/copy bursts aligned to other individual edits. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/test/rfc6902.test.ts`; `npm run typecheck -w zod-crud`, targeted RFC/history tests, `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`, full package tests, package smoke, docs evaluation, and `git diff --check` passed. Current benchmark snapshot: direct 50k append clone comparison showed `slice()+push` around 0.15ms vs `concat([value])` around 0.05ms; 50k individual add 100 around 5.7ms, down from about 30ms; 50k individual copy/move 100 around 5.5ms, down from about 18-25ms. |
| 065 | The raw container mutator still used spread append for array `set` at `/-` and at `idx === length`. Same-array document paths now avoid that, but public `applyOperation` and fallback RFC paths still paid the slower clone form on large arrays. | Switched raw array append mutation to `parent.concat([value])` and strengthened the RFC append test to prove the original array and retained element references are preserved. | Performance regression: 5 -> 5 with lower fallback/public array append overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/internal.ts`; `packages/zod-crud/test/rfc6902.test.ts`; `npm run typecheck -w zod-crud`, targeted RFC tests, direct 50k append clone comparison, `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`, full package tests, package smoke, docs evaluation, and `git diff --check` passed. Current benchmark snapshot: direct 50k append clone comparison showed `slice()+push` around 0.26ms vs `concat([value])` around 0.05ms; public `applyOperation(z.any(), array, add /-)` around 27.5ms under the full JSON boundary. |
| 066 | Same-array local validation still resolved the array parent and element schema for every operation path, and again for every copy/move source. Large burst patches against one array therefore paid repeated schema pointer cache lookups before the one-copy structural replay could run. | Kept the full schema lookup for the first operation, then validated later paths and copy/move sources by checking the pinned parent pointer and parsing only the final array index. This keeps the same-parent/same-element contract while avoiding repeated element-schema resolution inside one batch. | Performance regression: 5 -> 5 with lower same-array local validation overhead. Evidence: `packages/zod-crud/src/domain/schema/localPatch.ts`; `npm run typecheck -w zod-crud`; targeted RFC/history tests; `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`; full package tests, package smoke, docs evaluation, and `git diff --check` passed. Current benchmark snapshot: direct 50k batch comparison showed copy 100 append around 4.07ms vs previous 4.56ms, move 100 adjacent around 5.70ms vs previous 6.15ms, and copy50/move50 around 7.13ms vs previous 7.68ms; full core snapshot showed copy/move array batch 100 history=0 around 9.62ms, undo around 2.67ms, redo around 7.21ms. |
| 067 | `createJSONDocument` built and subscribed selection state even when selection was disabled, which is the default. Burst add/copy/move patches therefore still ran mutation auto-selection over every applied op, and hidden selection could become an implicit clipboard source despite `doc.selection` being undefined. | Only create the selection state and auto-rule subscription when `options.selection` enables it. Default documents now use `EMPTY_SELECTION` for metadata snapshots and omit selection fallback from checks/clipboard defaults. Added a regression proving default documents do not keep hidden selection targets after patches. | Performance regression: 5 -> 5 with default document burst edit overhead removed. Evidence: `packages/zod-crud/src/application/document/createJSONDocument.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`; targeted document-history tests; `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`; full package tests, package smoke, docs evaluation, and `git diff --check` passed. Current benchmark snapshot: direct 50k copy50/move50 `doc.patch` with selection disabled is around 0.32ms vs selection enabled around 7.43ms; full core copy/move batch 100 history=0 is around 0.33ms, down from 9.62ms; history=100 around 0.57ms, down from 9.86ms; undo around 0.10ms and redo around 0.19ms. |
| 068 | Selection-enabled burst edits still paid the full auto-target collector even in `single` selection mode. That collector tracked every add/copy/move destination through the remaining suffix, so a 100-op copy/move burst spent milliseconds computing targets that `single` mode would discard. | Added a primary auto-target path that scans from the end and tracks only the final surviving add/copy/move destination for `single` mode, while falling back to the full collector if that final target is removed by later ops. The multiple/extended modes keep the full target list behavior. | Performance regression: 5 -> 5 with selection-enabled burst edit overhead reduced for the default `single` mode. Evidence: `packages/zod-crud/src/domain/tracking/pointer.ts`; `packages/zod-crud/src/domain/selection/index.ts`; `packages/zod-crud/tests/selection-headless.test.ts`; `npm run typecheck -w zod-crud`; targeted selection/tracking/history tests; `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`; full package tests, package smoke, docs evaluation, and `git diff --check` passed. Current benchmark snapshot: direct 50k copy50/move50 `doc.patch` with `selection: true` is around 0.30ms, down from 7.43ms; `pickPrimaryAutoTarget` is around 0.00ms vs full `pickAutoTargets` around 7.04ms on the same applied patch; multiple selection remains around 7.69ms because it intentionally keeps every target. |
| 069 | Multiple/extended selection still needed every auto-selected add/copy/move destination, but the collector recomputed each final target by replaying the remaining suffix. Same-array bursts therefore stayed O(n²) even though all target movement can be represented as array index shifts. | Added a same-array auto-target collector that simulates target indices once across add/remove/copy/move operations, including removed targets and move remove-then-add semantics, and falls back to the generic suffix tracker for other patch shapes. Added tracking and multiple-selection regressions for surviving target lists. | Performance regression: 5 -> 5 with multiple-selection burst edit overhead reduced. Evidence: `packages/zod-crud/src/domain/tracking/pointer.ts`; `packages/zod-crud/test/track.test.ts`; `packages/zod-crud/tests/selection-headless.test.ts`; `npm run typecheck -w zod-crud`; targeted selection/tracking/history tests; `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`; full package tests, package smoke, docs evaluation, and `git diff --check` passed. Current benchmark snapshot: direct 50k copy50/move50 auto-target collection is around 0.07ms vs previous suffix replay around 6.78ms; `doc.patch` with `selection: { mode: "multiple" }` is around 0.37ms, down from about 7.69ms; full core default copy/move batch 100 history=0 remains around 0.32ms. |
| 070 | The main core benchmark still did not expose the selection-enabled copy/move burst paths that loops 068-069 optimized. A future regression in single or multiple selection auto-target handling could hide behind the default `history=0` batch row. | Added `selection=single` and `selection=multiple` copy/move batch rows to `perf:core`, keeping the existing default and history rows intact. A JSON-boundary fast-path experiment was measured and rejected because it did not improve the built benchmark consistently. | Performance regression: 5 -> 5 with better visibility for selection-enabled burst edits. Evidence: `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`; `node --check scripts/benchmark-core.mjs`; `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`; full package tests, package smoke, docs evaluation, and `git diff --check` passed. Current benchmark snapshot: 50k copy/move batch 100 shows history=0 around 0.34ms, selection=single around 0.36ms, selection=multiple around 0.60ms, and history=100 around 1.24ms. |
| 071 | Same-array history inverse calculation still cloned the whole array even when a copy/move/add batch had no removes. For 50k-item copy/move bursts, inverse generation only needed index validation and final undo ops, but still paid an O(array length) `slice()`. | Added a no-remove same-array inverse path that tracks current length and emits inverse remove/move ops without cloning the array, while preserving the existing value-capturing path for remove batches. Added a history regression for remove-free copy/move batches. | Performance regression: 5 -> 5 with lower copy/move history overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/inverse.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance rfc6902`; `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core` passed. Current benchmark snapshot: 50k copy/move array batch 100 history=100 is around 0.39ms, down from the loop 070 snapshot around 1.24ms; history=0 is around 0.33ms; undo is around 0.10ms and redo around 0.20ms. |
| 072 | Inverse assembly still used repeated `unshift()` to prepend each generated undo op. That keeps normal 100-op bursts cheap enough, but it turns history recording for larger same-array or fallback batches into avoidable O(n²) array movement. | Changed inverse assembly to append generated inverse ops in forward order and reverse once after successful validation. This preserves undo order while making inverse list construction linear for large batches. | Performance regression: 5 -> 5 with lower large-batch inverse assembly overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/inverse.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance rfc6902`; `PERF_ITEMS=50000 PERF_INDIVIDUAL=1000 PERF_ROUNDS=11 npm run perf:core`; `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`; full package tests, package smoke, docs evaluation, and `git diff --check` passed. Current 1000-op snapshot: 50k mixed array batch history=100 around 1.71ms, copy/move array batch history=100 around 2.04ms, undo mixed around 0.72ms, and redo mixed around 0.61ms. |
| 073 | Multiple selection auto-targeting still rebuilt a `SelectionSnap` with quadratic duplicate checks: `normalizeRanges` searched existing ranges for each new collapsed string range, and `collectSelectedPointers` used `includes` for each selected pointer. This showed up when a 1000-op copy/move burst intentionally selected every surviving target. | Added Map/Set-backed dedupe for collapsed string ranges and selected pointers while keeping object/text range handling on the existing equality semantics. Existing selection regressions cover the public result. | Performance regression: 5 -> 5 with lower multiple-selection burst overhead. Evidence: `packages/zod-crud/src/domain/selection/index.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- selection-headless document-selection-react`; `PERF_ITEMS=50000 PERF_INDIVIDUAL=1000 PERF_ROUNDS=11 npm run perf:core`; `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core` passed. Current 1000-op snapshot: 50k copy/move batch `selection=multiple` around 3.26ms, down from about 5.1ms; 100-op `selection=multiple` around 0.54ms. |
| 074 | Very large replace bursts often target one field across many rows, e.g. `/items/{i}/done`. The generic independent-replace path still parsed and sorted every full pointer, built a Map-based replace tree, and read every target individually even though this shape can be applied by resolving the array once. | Added a same-array field replace fast path in trusted patch replay and inverse calculation. It handles unique `/array/{index}/{field}` replace batches by cloning the array once and each touched row once, while repeated paths, nested replaces, mixed fields, and unsupported containers fall back to the generic ordered path. Added RFC sharing/ordered-semantics coverage and document history restore coverage. | Performance regression: 5 -> 5 with lower very-large field-edit burst overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/src/foundation/json-patch/inverse.ts`; `packages/zod-crud/test/rfc6902.test.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- rfc6902 document-history-performance`; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=11 npm run perf:core`; `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core` passed. Current 10k replace snapshot: 50k `doc.patch batch 10000 history=0` around 13.3ms, `history=100` around 18.1ms, undo around 5.95ms, and redo around 5.83ms. |
| 075 | The new same-array field replace replay path reduced structural copy work, but document-local schema validation still treated the same 10k field edits as unrelated independent replaces. That repeated pointer-to-schema lookup for `/items/0/done`, `/items/1/done`, and so on even though all edits share the same field schema. | Added a local-validation fast path for unique same-array field replace batches. It detects the same `/array/{index}/{field}` shape before the generic independent-replace path, applies the trusted patch, resolves the field schema once, and validates each value against that schema while preserving atomic rollback for invalid values. Added a plain-structural regression proving root schema parse is not rerun and invalid values are rejected atomically. | Performance regression: 5 -> 5 with lower very-large document field-edit validation overhead. Evidence: `packages/zod-crud/src/domain/schema/localPatch.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance rfc6902`; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=11 npm run perf:core`; `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core` passed. Current 10k replace snapshot: 50k `doc.patch batch 10000 history=0` around 11.95ms, `history=100` around 18.6ms, undo around 5.98ms, and redo around 6.06ms. |
| 076 | Clipboard paste/cut preflight already produced an accepted draft state, but clipboard commit still applied the same patch again through `ops.patch`. This duplicated structural replay for short burst paste workflows, especially spread paste payloads. | Carried normalized `applied` patches through preflight, paste, and cut results, then let document clipboard commit the previewed `next` state with `trustedApply` while preserving history recording, selection updates, and normalized applied patch reporting. Added a spread-paste row to the core benchmark. | Performance regression: 5 -> 5 with lower clipboard burst paste replay cost. Evidence: `packages/zod-crud/src/domain/schema/preFlight.ts`; `packages/zod-crud/src/domain/verbs/paste.ts`; `packages/zod-crud/src/domain/verbs/cut.ts`; `packages/zod-crud/src/application/document/clipboard.ts`; `packages/zod-crud/src/application/document/createJSONDocument.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance create-json-document document-clipboard-react`; `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`; full package tests and package smoke passed. Current snapshot: 50k `doc.clipboard.pastePayload spread 100` around 0.51ms; direct 50k spread 1000 comparison dropped from about 4.21ms to about 2.58ms. |
| 077 | Very deep history stacks still used the pure history reducer shape inside `createJSONDocument`, so every commit, undo, and redo copied the undo/redo arrays. This made "large undo/redo" depth cost grow sharply even for tiny patches. | Added mutable history-stack helpers for the document facade and kept the existing pure reducer helpers intact. Document history now peeks before restore, mutates the stack only after the accepted replay succeeds, and preserves merge, load/reset clearing, metadata, and depth behavior. Added a large-depth order regression and a benchmark row for 10k history entries. | Performance regression: 5 -> 5 with lower large history-depth overhead. Evidence: `packages/zod-crud/src/foundation/history.ts`; `packages/zod-crud/src/application/document/createJSONDocument.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance document-history-metadata document-interface load-history`; `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core` passed. Direct depth comparison: 20k commit dropped from about 528ms to about 40ms, undo all from about 627ms to about 17ms, redo all from about 623ms to about 13ms. Core snapshot: history depth 10k commit around 12.9ms, undo all around 6.2ms, redo all around 4.9ms. |
| 078 | Large history depth improved, but capped histories still shifted the whole undo array whenever commits exceeded the limit. Long editing sessions with a 10k or 20k history cap therefore kept paying O(limit) trimming on every extra commit. | Changed the mutable document history stack to keep an `undoStart` offset for discarded entries and compact the prefix only occasionally. Updated document history depth/capability reads to use mutable-stack depth helpers, added capped-history undo/redo order coverage, and extended the core benchmark with a history overflow row. | Performance regression: 5 -> 5 with lower capped-history overflow commit cost. Evidence: `packages/zod-crud/src/foundation/history.ts`; `packages/zod-crud/src/application/document/createJSONDocument.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance document-history-metadata document-interface document-check load-history`; `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core` passed. Direct overflow comparison: limit 10k with 50k commits dropped from about 380ms to about 65ms, and limit 20k with 60k commits dropped from about 643ms to about 67ms. Core snapshot: history limit 10k overflow 50k commits around 52.7ms. |
| 079 | `doc.canPaste()` cloned the clipboard payload through `clipboard.read()` before checking paste feasibility. For large clipboard buffers this made a read-only capability check pay the same defensive clone cost that only public reads need. | Added an internal symbol-based clipboard peek path and changed document `canPaste` to use it, keeping the public `clipboard.read()` clone contract unchanged. Added a core benchmark row for large clipboard-backed `canPaste` so this path remains visible. | Performance regression: 5 -> 5 with lower large clipboard capability-check overhead. Evidence: `packages/zod-crud/src/application/document/clipboard.ts`; `packages/zod-crud/src/application/document/createJSONDocument.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- create-json-document document-clipboard-react document-history-performance document-interface`; `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core` passed. Direct 50k comparison: `canPaste({ replace: "/items" })` from clipboard dropped from about 66ms to about 38ms; `canPastePayload` on the same payload is around 31ms. Core snapshot: `doc.canPaste clipboard replace /items` around 33.5ms. |
| 080 | Clipboard-backed `doc.canPaste({ replace: "/items" })` still revalidated the large clipboard payload as JSON during patch preflight even though document-owned clipboard buffers are already validated or cloned at write/copy/cut time. | Added a narrow trusted-value preview path for single internal `add`/`replace` paste patches. Document `canPaste` and internal `clipboard.paste` use it only when the payload comes from the internal clipboard buffer; public `canPastePayload` and public `clipboard.read()` keep the normal JSON boundary. | Performance regression: 5 -> 5 with lower large clipboard replace-check overhead and unchanged external validation. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/src/application/document/createJSON.ts`; `packages/zod-crud/src/application/document/check.ts`; `packages/zod-crud/src/application/document/clipboard.ts`; `packages/zod-crud/src/application/document/createJSONDocument.ts`; `npm run typecheck -w zod-crud`; targeted document tests; full package tests; package smoke; docs evaluation; `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`; `git diff --check` passed. Direct 50k comparison: internal clipboard `canPaste({ replace: "/items" })` averaged about 5.0ms while external `canPastePayload({ replace: "/items" }, items)` stayed around 32.2ms. Core snapshot: `doc.canPaste clipboard replace /items` around 8.1ms average with 3.0ms minimum. |
| 081 | Large same-array same-field replace batches still parsed every JSON Pointer fully while applying the batch and while computing history inverses. On 10k `/items/{i}/done` edits this left avoidable string parsing in the short-burst and undo/redo path. | Added a fast unescaped pointer parser for same-array field replace paths, with escaped paths falling back to the RFC 6901 parser. Added history coverage for escaped field names so `~1` paths keep undo/redo behavior. | Performance regression: 5 -> 5 with lower large field-edit history overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/src/foundation/json-patch/inverse.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`; targeted RFC/history tests; full package tests; package smoke; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=7 npm run perf:core`; `git diff --check` passed. Direct 50k/10k comparison: `computeInverses` for same-array field replace averaged about 2.4ms, down from about 5.9ms; trusted apply averaged about 5.9ms. Core snapshot: 10k batch undo/redo dropped to about 3.6ms from the prior 5.9-6.0ms local run. |
| 082 | Large history depth still paid the generic inverse path for every single accepted patch. Tiny 10k-50k edit histories are common undo/redo stress cases, and the single-op path was still allocating replace-batch helper arrays and sorting one path before recording each entry. | Added a single-op inverse fast path and a simple unescaped pointer reader for remove/replace inverse lookup, while escaped pointers still fall back to the RFC parser. Existing large-depth and undo/redo tests cover behavior. | Performance regression: 5 -> 5 with lower single-edit history recording overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/inverse.ts`; existing `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`; targeted RFC/history tests; full package tests; package smoke; `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`; `git diff --check` passed. Direct comparison: 500k single replace inverses dropped from about 68.6ms after the first fast path to about 25.1ms. Core snapshot: history depth 10k commit around 11.5ms and history overflow 50k commits around 50.2ms. |
| 083 | Undo/redo replay of accepted single edits still used the same trusted patch function that local schema preflight uses, so history restore kept operation-shape and value-validation checks that were already proven at commit time. | Added an accepted-history replay path for single `add`/`replace` entries and routed document `trustedPatch` through it. The stricter `applyTrustedPatch` remains unchanged for local preflight callers. Added a single-segment trusted mutation shortcut for common root-field edits. | Performance regression: 5 -> 5 with lower single-edit undo/redo replay overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/src/application/document/createJSON.ts`; existing history/RFC tests; `npm run typecheck -w zod-crud`; targeted history/RFC tests; full package tests; package smoke; `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`; `git diff --check` passed. Direct comparison: 500k single `/value` accepted replays averaged about 47.5ms vs `applyTrustedPatch` around 131ms. Core snapshot: history depth 10k undo all around 3.3ms and redo all around 2.7ms. |
| 084 | The accepted-history single edit replay path still delegated root-object field writes to the generic container mutator. For common tiny history entries such as `/value`, that kept avoidable branch and parser overhead in undo/redo even after replay skipped validation. | Added a root-object single-segment mutation path that directly clones the object and assigns the accepted value, preserving own `__proto__` data keys without prototype pollution. Arrays and deeper pointers keep the existing container path. | Performance regression: 5 -> 5 with lower root-field undo/redo replay overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; existing history/RFC tests; `npm run typecheck -w zod-crud`; targeted history/RFC tests; full package tests; package smoke; `PERF_ITEMS=50000 PERF_ROUNDS=9 npm run perf:core`; `git diff --check` passed. Direct comparison: 500k single `/value` accepted replays averaged about 29.1ms, down from about 47.5ms. Direct 20k history replay measured undo around 3.9ms and redo around 3.7ms. |
| 085 | Same-array same-field local schema validation still parsed every `/items/{i}/field` pointer with the full RFC 6901 parser, even though the trusted patch replay and inverse paths already had a fast unescaped parser for that common shape. This left avoidable parser work in large short-burst edit checks. | Added the same narrow unescaped array-field parser to document-local schema validation, with escaped paths still falling back to the RFC parser. Existing escaped-field history coverage verifies fallback behavior. | Performance regression: 5 -> 5 with lower 10k field-edit validation overhead. Evidence: `packages/zod-crud/src/domain/schema/localPatch.ts`; existing history/RFC tests; `npm run typecheck -w zod-crud`; targeted history/RFC tests; full package tests; package smoke; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=7 npm run perf:core`; `git diff --check` passed. Direct 50k/10k local validation averaged about 7.7ms, down from about 8.9ms; core 10k `doc.patch` batch snapshot was around 7.3ms with history off and 10.7ms with history on. |
| 086 | Accepted undo/redo replay for 10k same-array same-field replace batches still used the trusted patch path that checks every replayed value for JSON serializability. History entries were already accepted at commit time, so this repeated a boundary check without improving replay safety. | Routed accepted batch replay through the same-array field replace fast path with trusted values, while keeping the normal `applyTrustedPatch` and local preflight value checks unchanged. | Performance regression: 5 -> 5 with lower large batch undo/redo replay overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `npm run typecheck -w zod-crud`; targeted history/RFC tests; full package tests; package smoke; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=7 npm run perf:core` passed. Direct 50k/10k replay comparison: `applyAcceptedPatch` averaged about 3.15ms versus `applyTrustedPatch` about 4.32ms; document history undo/redo p50 measured about 2.9ms. |
| 087 | Large append bursts were still not visible in `perf:core`, and the local same-array structural validation path rechecked add values for JSON serializability after it had already validated those same values before replay. This left avoidable boundary work in 10k add batches and accepted add redo. | Added trusted-value replay options for internal paths that have already checked values, used it for accepted replay and locally validated same-array structural batches, and added benchmark rows for 10k append batch commit plus undo/redo. Kept the default trusted patch path and public JSON guards intact with a same-array field replace regression. | Performance regression: 5 -> 5 with lower append burst replay overhead and better benchmark visibility. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/src/domain/schema/localPatch.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`; targeted history/RFC tests; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=7 npm run perf:core` passed. Direct 50k/10k add comparison: local validation averaged about 10.0ms, down from about 12.0ms; accepted add replay averaged about 3.6ms, down from about 5.6ms. Core now reports 10k add batch commit and history restore rows. |
| 088 | Append-only add batches still computed history inverses through the general same-array structural path. For 10k `/items/-` edits, inverse generation repeatedly parsed the same parent path even though the undo list is deterministically the appended indexes in reverse order. | Added an append-only add inverse path that validates one append parent, reads the array length once, and emits reverse remove operations directly. Added a history regression proving append-only batches restore in order without schema revalidation. | Performance regression: 5 -> 5 with lower append history recording overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/inverse.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`; targeted history/RFC tests; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=7 npm run perf:core` passed. Direct 50k/10k inverse comparison: `computeInverses` p50 dropped from about 3.05ms to about 0.99ms; core 10k add batch `history=100` averaged about 10.7ms, down from the prior local snapshot around 16.6ms. |
| 089 | The same-array same-field local validation path parsed every `/items/{i}/field` path once to choose the field schema and then replayed the batch through `applyTrustedPatch`, which parsed the same paths again. For 10k field edits, the duplicate parse/apply setup was now a large share of commit time. | Reused the parsed same-array field replace metadata to validate target rows, run the JSON guard and field schema validation, and apply the immutable row replacements directly. The fallback paths still handle unsupported shapes and public JSON errors remain guarded. | Performance regression: 5 -> 5 with lower 10k field-edit commit latency. Evidence: `packages/zod-crud/src/domain/schema/localPatch.ts`; existing history/RFC and JSON-guard tests; `npm run typecheck -w zod-crud`; targeted history/RFC tests; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=7 npm run perf:core` passed. Direct 50k/10k local validation p50 dropped from about 6.05ms to about 3.48ms; core `doc.patch batch 10000 history=0` averaged about 4.08ms and `history=100` about 6.59ms. |
| 090 | Append-only add replay still used the general same-array structural apply path. Even after inverse generation improved, accepted 10k redo and locally validated append commits still reparsed the same `/-` parent for every op before pushing values one by one. | Added an append-only add apply fast path that validates a single append parent, checks add values when needed, concatenates the new values once, and emits concrete applied paths. Added RFC coverage for multi-append applied path normalization. | Performance regression: 5 -> 5 with lower append replay and commit latency. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/test/rfc6902.test.ts`; `npm run typecheck -w zod-crud`; targeted RFC/history tests; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=7 npm run perf:core` passed. Direct 50k/10k replay comparison: accepted append replay p50 dropped to about 0.99ms from the prior local snapshot around 3.6ms; local append validation p50 was about 7.87ms; core `history redo add batch 10000` averaged about 1.01ms. |
| 091 | Same-array field replace inverse generation used a `Set` for duplicate-index detection on every batch. The common 10k `/items/0..9999/done` burst is ordered and unique, so this paid Set allocation and insertion work even though duplicates can only appear when the index order stops increasing. | Made duplicate detection lazy for same-array field replace inverses: ordered indexes avoid the Set, while unordered batches allocate it on demand and still reject repeated indexes. Added an unordered-index history regression. | Performance regression: 5 -> 5 with lower same-field history recording overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/inverse.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`; targeted history/RFC tests; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=7 npm run perf:core` passed. Direct 50k/10k inverse p50 dropped from about 2.02ms to about 1.70ms. |
| 092 | Same-array field replace replay still used a `Set` for duplicate-index detection on every accepted batch. The common ordered 10k replay path therefore kept allocation and insertion overhead even after inverse calculation stopped paying it. | Applied the same lazy duplicate detection to same-array field replace replay and added RFC coverage for unordered unique indexes. Ordered replay avoids the Set, while unordered batches still detect repeated indexes and fall back to ordered semantics when needed. | Performance regression: 5 -> 5 with lower accepted 10k replay overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/test/rfc6902.test.ts`; `npm run typecheck -w zod-crud`; targeted RFC/history tests; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=7 npm run perf:core` passed. Direct 50k/10k accepted replay p50 dropped from about 4.0ms to about 2.6ms; core `history redo batch 10000` averaged about 2.62ms. |
| 093 | Same-array field replace replay and local validation still collected all parsed operations before applying row replacements. For ordered 10k field edits, that meant an intermediate item list plus a second row-application loop even though failures can discard a local draft without touching the original state. | Streamed same-array field replacement into a local cloned array during replay, and merged local schema validation with row replacement so values are checked and applied in one pass over the parsed items. Existing duplicate, unordered, escaped, and JSON-guard tests cover the preserved semantics. | Performance regression: 5 -> 5 with lower 10k field-edit replay and commit latency. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/src/domain/schema/localPatch.ts`; existing RFC/history tests; `npm run typecheck -w zod-crud`; targeted RFC/history tests; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=7 npm run perf:core` passed. Direct 50k/10k local validation p50 was about 3.31ms and accepted replay p50 about 2.25ms; core `doc.patch batch 10000 history=100` averaged about 6.13ms. |
| 094 | Append-only add undo replay still removed tail items through the general same-array structural loop. The undo ops are deterministic `/items/N-1`, `/items/N-2`, ... removals, so 10k undo paid per-op array mutation setup even though the final array is one prefix slice. | Added a tail-remove batch fast path for same-parent descending array removes and guarded it with RFC sharing coverage. Simple paths use the cheap parser, escaped parents use the existing RFC parser inside detection, and unsupported order or mixed parents still fall back to the existing RFC path. | Performance regression: 5 -> 5 with lower append-batch undo replay latency. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/test/rfc6902.test.ts`; `npm run typecheck -w zod-crud`; targeted RFC/history tests; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=7 npm run perf:core` passed. Direct 50k/10k tail remove replay p50 dropped from about 3.07ms to about 0.98ms for trusted replay and from about 2.97ms to about 0.76ms for accepted replay; core `history undo add batch 10000` averaged about 1.28ms. |
| 095 | Same-array field replace local validation still parsed the whole batch into an intermediate item list and allocated a duplicate-index `Set` immediately. Ordered 10k field edits are unique by construction, so commit validation still paid avoidable allocation after replay and inverse paths had already moved to lazy duplicate detection. | Streamed local same-array field replacement directly into the cloned array and made duplicate detection lazy for ordered indexes. Added an unordered local-validation regression so non-monotonic unique batches still avoid root schema parsing. | Performance regression: 5 -> 5 with lower 10k field-edit commit validation latency. Evidence: `packages/zod-crud/src/domain/schema/localPatch.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`; targeted RFC/history tests; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=7 npm run perf:core` passed. Direct 50k/10k local validation p50 dropped from about 3.32ms to about 2.74ms; core `doc.patch batch 10000 history=0` averaged about 3.00ms. |
| 096 | Internal clipboard `canPaste({ replace: "/items" })` still validated a copied payload against the same target schema. For same-source replace on plain structural schemas, the payload already came from that exact schema path and only path existence needs checking; external `clipboard.write` payloads must still be validated. | Marked clipboard buffers that come from document copy/cut as schema-trusted, routed trusted-value preview through local validation before full-root single-value replay, and added a narrow same-source replace canPaste fast path. Added coverage proving external writes with a spoofed source still run schema validation. | Performance regression: 5 -> 5 with lower large clipboard same-source capability-check latency. Evidence: `packages/zod-crud/src/application/document/clipboard.ts`; `packages/zod-crud/src/application/document/createJSON.ts`; `packages/zod-crud/src/application/document/createJSONDocument.ts`; `packages/zod-crud/src/domain/schema/localPatch.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`; targeted document tests; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=7 npm run perf:core` passed. Direct 50k same-source canPaste p50 dropped from about 3.19ms to about 0.001ms while external write stayed around 3.21ms p50; core `doc.canPaste clipboard replace /items` averaged about 0.03ms. |
| 097 | Same-array field replace inverse generation still built an intermediate parsed item list before producing inverse replace operations. The ordered 10k history-recording path only needs the previous field value and final inverse order, so the item list and separate reverse pass were avoidable allocation. | Streamed inverse generation while parsing same-array field replace ops, kept lazy duplicate detection with index history, and filled the inverse array in final undo order for the common no-`test` case. Existing unordered, escaped, and history restore regressions cover semantics. | Performance regression: 5 -> 5 with lower same-field history-recording overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/inverse.ts`; existing RFC/history tests; `npm run typecheck -w zod-crud`; targeted RFC/history tests; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=7 npm run perf:core` passed. Direct 50k/10k `computeInverses` p50 dropped from about 1.57ms to about 1.46ms for ordered field edits and from about 1.93ms to about 1.71ms for unordered field edits; core `doc.patch batch 10000 history=100` averaged about 5.07ms. |
| 098 | Same-array field replace replay and validation avoided duplicate-index `Set` allocation for ascending ordered batches, but undo replays inverse patches in descending index order. Those descending unique batches still allocated a `Set` from the second op onward even though they are just as monotonic and duplicate-free as the original forward edit. | Generalized duplicate detection to treat strictly ascending and strictly descending index streams as monotonic. Replay, inverse generation, and local validation now allocate a `Set` only for equal indexes or when the order changes direction. Existing unordered and duplicate-path regressions cover fallback behavior. | Performance regression: 5 -> 5 with lower large same-field undo replay overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/src/foundation/json-patch/inverse.ts`; `packages/zod-crud/src/domain/schema/localPatch.ts`; existing RFC/history tests; `npm run typecheck -w zod-crud`; targeted RFC/history tests; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=7 npm run perf:core` passed. Direct 50k/10k accepted descending replay p50 dropped from about 2.52ms to about 2.08ms, and core `history undo batch 10000` averaged about 3.73ms. |
| 099 | Large clipboard copy/read/write paths still cloned already-validated plain JSON through the conservative trusted clone, paying an own-property check for every object. Large external array writes also validated first and then paid the same conservative clone cost. | Added `cloneTrustedPlainJson`, a fast clone for values that have already crossed this module's JSON boundary. It skips own-property checks only while `Object.prototype` has no enumerable keys, otherwise falls back to the defensive clone. Routed large-array `cloneJsonSerializable`, document copy/cut payloads, and clipboard reads through it, and added a regression for enumerable `Object.prototype` keys. | Performance regression: 5 -> 5 with lower large clipboard clone latency while keeping public JSON guards. Evidence: `packages/zod-crud/src/foundation/json.ts`; `packages/zod-crud/src/domain/verbs/copy.ts`; `packages/zod-crud/src/domain/verbs/cut.ts`; `packages/zod-crud/src/application/document/clipboard.ts`; `packages/zod-crud/tests/create-json-document.test.ts`; `npm run typecheck -w zod-crud`; targeted clipboard/document tests; full package tests; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=7 npm run perf:core` passed. Direct 50k helper comparison: `cloneTrustedJson` p50 about 5.24ms vs `cloneTrustedPlainJson` p50 about 4.86ms; direct clipboard write p50 about 29.72ms, down from the prior local 36.93ms run. Core snapshot: `doc.clipboard.copy /items` avg about 5.43ms, write avg about 33.32ms, read avg about 5.43ms. |
| 100 | The core benchmark reported average, min, and max only. Large clipboard and history rows can have GC or scheduler outliers, so averages alone made short-burst scoring less stable even when p50 stayed healthy. A single-pass external-array clone experiment was also worth checking before weakening any public JSON boundary. | Added p50 reporting to `bench` and `benchWithSetup` through a shared sample-stat helper. Measured a one-pass validate-and-clone experiment for 50k external arrays and rejected it because it was slower than the existing validation plus trusted clone path, so no runtime JSON-boundary change was kept. | Performance regression evidence: 5 -> 5 with better benchmark signal and unchanged public safety. Evidence: `scripts/benchmark-core.mjs`; `PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=7 npm run perf:core` passed and now prints p50 for sampled rows. Current snapshot: `doc.clipboard.write items payload` avg about 36.99ms and p50 about 34.31ms; `doc.patch batch 10000 history=0` avg about 3.00ms and p50 about 2.95ms; `history undo add batch 10000` avg about 5.02ms and p50 about 2.04ms, showing why p50 belongs in the report. |
| 101 | The previous benchmark still under-described two user-facing questions: what happens when a short burst is expressed as many individual edits, and whether a transaction makes huge undo/redo cheap. | Added repeated p50 measurements for individual replace/add/remove/copy/move bursts, transaction-wrapped individual bursts, and undo/redo over many individual entries. The new evidence shows that transaction merges history cost but cannot remove the per-edit immutable array copy cost; `doc.patch([...])` remains the right surface for known bursts. Re-ran 50k/10k measurements and rejected public JSON-boundary shortcuts because they would weaken external-state safety. | Performance regression evidence: 5 -> 5 with clearer burst-edit guidance and a narrower next runtime candidate. Evidence: `scripts/benchmark-core.mjs`; `npm run build -w zod-crud`; `npm test -w zod-crud`; `PERF_GC=1 PERF_ITEMS=50000 PERF_BATCH=1000 PERF_INDIVIDUAL=1000 PERF_ROUNDS=9 node --expose-gc scripts/benchmark-core.mjs` passed. Current snapshot: 50k individual replace 1000 p50 about 77.70ms; undo 1000 individual entries p50 about 73.45ms; redo 1000 individual entries p50 about 71.37ms; transaction undo/redo p50 about 0.51/0.29ms; batch add/copy-move 1000 p50 about 0.84/1.06ms. |
| 102 | The next obvious local-validation micro-optimizations were tempting: reuse cycle-detection state across many values, or pre-check object enumerability with `Object.keys` to reduce per-field descriptor work. Both looked safe by type and targeted tests, but performance needed proof before keeping them. | Implemented both experiments locally, ran typecheck, targeted RFC/document-history tests, and focused 50k/10k benchmark rows, then reverted the code because root-object bulk rows regressed or became unstable. The reusable weak-marker attempt made root object add/replace worse; the `Object.keys` pre-check attempt improved some array rows but caused root object replace p50 spikes around 20ms. | Performance regression evidence: stronger negative result; no runtime change kept. Evidence: `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- rfc6902 create-json-document document-history-performance`; focused `PERF_GC=1 PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=9 node --expose-gc scripts/benchmark-core.mjs` runs. Current conclusion: do not tune known-JSON object validation by adding `WeakMap` token tracking or extra `Object.keys` passes; investigate root-object bulk history representation separately. |
| 103 | Root-object replace history restore still reached the root replace fast path only after `applyAcceptedPatch` fell through to the broader trusted replay. That made 10k root-key undo/redo pay extra dispatch work even though the values were already accepted by document commit. | Added the existing root-object replace fast path directly to `applyAcceptedPatch` with trusted values, keeping the same replay semantics while avoiding the fallback pass for accepted history entries. | Performance regression evidence: 5 -> 5 with lower 10k root-key replace replay cost. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance`; `npm run build -w zod-crud`; focused `PERF_GC=1 PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=9 node --expose-gc scripts/benchmark-core.mjs` showed `accepted root object replace batch 10000` p50 about 3.95ms vs the prior same-command 10.15ms and `history undo root object replace batch 10000` p50 about 7.25ms vs 39.42ms. A wider 7-round check kept array history rows in range and reported root replace undo/redo p50 about 8.22ms/5.61ms. |
| 104 | Full root-object replace batches still copied every old root value and then overwrote every key. For the common ordered full-replace shape, that is avoidable work; a separate `Set`-to-object inverse experiment was measured and reverted because add/remove rows became unstable. | Added an ordered full root-object replace fast path that builds the next root object directly when patch order exactly matches existing root key order, with the existing copy-and-overwrite path as fallback. Added a regression proving the direct path keeps own `__proto__` as data and preserves key order. | Performance regression evidence: 5 -> 5 with lower ordered 10k root replace commit cost. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/tests/apply-patch.test.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- apply-patch document-history-performance`; `npm run build -w zod-crud`; repeated focused `PERF_GC=1 PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=9 node --expose-gc scripts/benchmark-core.mjs` runs showed `doc.patch root object replace batch 10000 history=100` p50 about 10.73-10.97ms versus the prior 13.38ms reference, and `history=0` p50 about 6.69-9.08ms versus the prior 9.46ms reference. |
| 105 | Accepted same-array field replace replay treated every descending index stream as out-of-order, so undoing an ascending 10k field batch allocated a duplicate-detection `Set` even though inverse patches are strictly descending and unique. | Generalized same-array field replace replay to treat strictly ascending and strictly descending index streams as monotonic. Only repeated indexes or direction changes use the existing last-write-wins `Set` path. | Performance regression evidence: 5 -> 5 with lower huge undo/redo burst cost. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; existing same-array unordered/repeated/history tests; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance apply-patch`; `npm run build -w zod-crud`; focused `PERF_GC=1 PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=9 node --expose-gc scripts/benchmark-core.mjs` reported `history undo batch 10000` p50 about 3.08ms and `history redo batch 10000` p50 about 1.93ms after a previous full-core run showed a 211.38ms undo p50 spike on the same row. |
| 106 | Same-array element replace local validation still reparsed the array parent for every `/items/{i}` op. The first op already proves the parent array and element schema, so the remaining ops only need to confirm the same encoded parent and parse the numeric index. | Reused the first element-replace parent as a prefix and parsed only the trailing index for subsequent ops, while keeping the full parser fallback for the first op and existing escaped-parent coverage. | Performance regression evidence: 5 -> 5 with lower 10k item replace commit and replay cost. Evidence: `packages/zod-crud/src/domain/schema/localPatch.ts`; existing same-array element replace tests including escaped parents and repeated indexes; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance apply-patch`; `npm run build -w zod-crud`; focused `PERF_GC=1 PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=9 node --expose-gc scripts/benchmark-core.mjs` reported `doc.patch item replace batch 10000 history=0/100` p50 about 3.76ms/4.99ms and undo/redo p50 about 1.25ms/0.82ms. |
| 107 | Large trusted clipboard copy/write/read paths still paid an own-property check for every cloned object even when `Object.prototype` had no enumerable keys. The documentation already described this as a guarded fast path, but the current helper always ran the conservative check. | Made `cloneTrustedPlainJson` decide once per clone whether `Object.prototype` has enumerable keys. Normal clean-prototype clones skip per-key `hasOwnProperty`; polluted-prototype clones keep the existing guarded loop so inherited enumerable keys are not copied. A split-function experiment was measured and rejected because trusted clipboard write p50 became unstable. | Performance regression evidence: 5 -> 5 with lower normal trusted clone overhead and unchanged polluted-prototype behavior. Evidence: `packages/zod-crud/src/foundation/json.ts`; existing clipboard clone isolation and enumerable `Object.prototype` regression tests; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- create-json-document`; full `npm test -w zod-crud`; `npm run build -w zod-crud`; `git diff --check`; focused `PERF_GC=1 PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=9 node --expose-gc scripts/benchmark-core.mjs` before/after showed `doc.clipboard.copy /items` p50 about 5.54ms -> 4.77ms, trusted write p50 about 5.29ms -> 5.07ms, document-items write p50 about 5.01ms -> 4.88ms, and `cloneJsonSerializable state` p50 about 70.36ms -> 44.02ms; read rows remained noise-sensitive around 4.7-5.1ms. |
| 108 | Accepted history replay for large root-object batches tried same-array replace fast paths before root-object fast paths. Root paths can be rejected or accepted from the first segment, so 10k root add/remove/replace undo-redo paid avoidable array-shape probing. | Moved root-object remove/add/replace replay ahead of same-array replace replay in `applyAcceptedPatch`, matching the cheaper dispatch order already used by trusted replay while keeping array paths on the same fall-through behavior. | Performance regression evidence: 5 -> 5 with lower root-object accepted replay overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance apply-patch`; `npm run build -w zod-crud`; focused `PERF_GC=1 PERF_ITEMS=50000 PERF_BATCH=10000 PERF_ROOT_KEYS=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=9 node --expose-gc scripts/benchmark-core.mjs` before/after showed `accepted root object add batch 10000` p50 about 6.06ms -> 3.57ms, accepted remove p50 about 3.78ms -> 2.26ms, accepted replace p50 about 2.95ms -> 2.86ms, and root add undo p50 about 3.67ms -> 2.95ms; redo remained around 5.7ms because replay still copies the expanded root object once. |
| 109 | Ordered full root-object replace inverse generation looked like a possible follow-up: the patch order often matches root key order, so the duplicate-key `Set` might be avoidable. But root replace history was already noisy enough that the change needed proof before keeping it. | Implemented a for-in ordered inverse experiment that scanned source keys and ops together, ran typecheck, targeted history tests, and focused root benchmarks, then reverted it because p50 did not improve consistently. | Performance regression evidence: no runtime change kept. Evidence: `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance apply-patch`; focused `PERF_GC=1 PERF_ITEMS=50000 PERF_BATCH=10000 PERF_ROOT_KEYS=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=9 node --expose-gc scripts/benchmark-core.mjs` after the experiment showed `doc.patch root object replace batch 10000 history=100` p50 about 7.85ms, undo p50 about 5.81ms, and accepted replay p50 about 3.14ms, which stayed within noise or worse than the prior accepted replay p50 around 2.86ms. Current conclusion: do not add ordered inverse scanning without a stronger history representation change. |
| 110 | Two small dispatch/traversal candidates remained tempting: replacing the rekey exact-conflict `walk` callback with a dedicated scanner, and rejecting non-remove root batches before `applyRootObjectRemovePatch` builds `Object.keys(state)`. Both looked mechanically cheaper, but each sits on noisy hot paths where V8 shape and branch behavior can dominate. | Implemented both experiments separately, ran typecheck and targeted tests, measured focused benchmark rows, and reverted both because neither produced stable p50 improvement. The rekey scanner variants made `doc.clipboard.pastePayload spread 100 + rekey` worse, and the root remove probe precheck failed to improve accepted root replay in direct or core measurements. | Performance regression evidence: stronger negative result; no runtime change kept. Evidence: `packages/zod-crud/src/domain/schema/rekey.ts` experiment passed `npm run typecheck -w zod-crud` and `npm test -w zod-crud -- create-json-document document-history-performance`, but focused 50k benchmark p50 moved `pastePayload spread 100 + rekey` from about 4.35ms to 4.68-4.88ms. `packages/zod-crud/src/foundation/json-patch/index.ts` experiment passed `npm run typecheck -w zod-crud` and `npm test -w zod-crud -- document-history-performance apply-patch`, but direct accepted replay reported add/replace/remove p50 around 5.06/4.27/4.14ms and full root rows were unstable or worse. Current conclusion: avoid callback-removal and first-op remove-probe micro-tuning; the next meaningful gains need fewer whole-object materializations or narrower validated surfaces. |
| 111 | Large root-object history undo/redo still materialized whole root objects by replaying 10k root-level patches. After loop 110, more dispatch micro-tuning had poor evidence; the meaningful remaining cost was whole-object materialization during restore. | Added a compact restore representation for large root-object add/remove/replace history entries: store the trusted `before` root snapshot for batches of 512+ root-key operations, capture `after` lazily when undo starts, and restore via `trustedApply` while preserving forward/inverse patch notifications, `lastPatch`, and selection/history metadata behavior. Added a regression that large root record undo/redo restores snapshots while subscribers still receive the original patch arrays. | Performance regression evidence: 5 -> 5 with root bulk undo/redo moved from millisecond replay to snapshot restore. Evidence: `packages/zod-crud/src/application/document/createJSONDocument.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance document-interface`; full `npm test -w zod-crud`; `git diff --check`; focused root benchmarks reported `history undo root object replace/add/remove batch 10000` p50 about 0.05/0.04/0.04ms and redo p50 about 0.02/0.03/0.02ms. Tradeoff: eligible history entries now retain the trusted `before` root snapshot, so root-object commit rows can be noisier; the change is limited to large root-level bulk edits. |
| 112 | The root bulk snapshot path captured `after` lazily on undo, but kept that large root object after redo moved the entry back to the undo stack. That made repeated history use retain both `before` and `after` snapshots even though `after` can be recaptured on the next undo. | Cleared `snapshot.after` after a successful redo and extended the large root record history regression to run a second undo/redo cycle, proving the lazy recapture still preserves restore semantics and patch metadata. | Performance regression evidence: stronger memory-retention behavior for the 111 snapshot path without changing public history semantics. Evidence: `packages/zod-crud/src/application/document/createJSONDocument.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance document-interface`; full `npm test -w zod-crud`; `npm run docs:evaluate`; `git diff --check` passed. |
| 113 | Root bulk snapshot restore now had strong p50 evidence, but its memory pressure was still inferred from implementation shape. That left future tuning blind to whether a change retained one large root state or accidentally kept stale post-redo states alive. | Added a retained-heap benchmark row to `perf:core`. It runs a root bulk add, undo, redo, then a small follow-up patch, holding the resulting document live across GC and reporting subject-retained heap per sample. The helper accumulates live subjects during measurement so V8 allocator reuse does not collapse the p50 to zero. | Performance regression evidence: stronger memory visibility for root bulk history. Evidence: `scripts/benchmark-core.mjs`; focused `PERF_GC=1 PERF_ITEMS=50000 PERF_BATCH=10000 PERF_ROOT_KEYS=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=3 node --expose-gc scripts/benchmark-core.mjs` reported `history root object add redo then small patch retained heap 10000` p50 about 3.49MB while undo/redo p50 stayed about 0.05/0.04ms. A 50k-root check reported retained heap p50 about 23.03MB. |
| 114 | Large stable replace batches with a single string selection still built a `Set` of all 10k replacement paths just to prove no replacement was a strict ancestor of the selected path. That allocation is unnecessary for the common single-caret/selection case where the selected targets are few and ordinary JSON Pointer strings. | Added a small-string-selection fast path for stable replace batches. It scans replacement paths directly, keeps the existing `Set` fallback for fragment/escaped pointers, non-string points, and larger selections, and added a regression proving leaf replacements keep a single selection while replacing its ancestor clears it. | Performance regression evidence: 5 -> 5 with lower selection overhead for large stable leaf replace batches. Evidence: `packages/zod-crud/src/domain/selection/index.ts`; `packages/zod-crud/tests/selection-headless.test.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- selection-headless document-history-performance`; `npm run build -w zod-crud`; focused 50k/10k benchmark comparison showed `doc.patch batch 10000 selection=single` p50 about 6.61ms before vs about 5.17-5.53ms after, while `history=0` stayed around 4-7ms under the same noisy command. |
| 115 | The new small-selection fast path still excluded escaped JSON Pointer text and fell back to the large replacement `Set`. But encoded JSON Pointer strings preserve segment boundaries with literal `/`, so valid `~0`/`~1` escaped pointers can use the same strict-prefix string check safely. | Broadened the small-selection stable replace fast path to accept valid JSON Pointer escape text, while fragment pointers and invalid `~` escapes still fall back. Added escaped selection regressions and a `perf:core` row for 10k escaped stable replaces with a single selection. | Performance regression evidence: stronger escaped-pointer selection coverage and lower escaped stable replace selection overhead. Evidence: `packages/zod-crud/src/domain/selection/index.ts`; `packages/zod-crud/tests/selection-headless.test.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- selection-headless`; `npm run build -w zod-crud`; focused 50k/10k benchmark comparison showed `doc.patch escaped batch 10000 selection=single` p50 about 32.41ms before vs about 9.59-16.22ms after. |
| 116 | Escaped same-array field replace batches still parsed the full JSON Pointer on every op in local validation, accepted replay, and inverse generation. The first op already proves the encoded array parent and encoded field suffix, so the remaining ops only need the numeric row index between those stable text boundaries. | Cached raw array-field prefix/suffix text after the first parsed op and reused it to parse only the row index for the rest of the batch across local schema validation, foundation replay, and history inverse generation. Added a regression covering escaped array parents plus escaped field names without root schema validation. | Performance regression evidence: 5 -> 5 with lower escaped same-array field batch overhead. Evidence: `packages/zod-crud/src/domain/schema/localPatch.ts`; `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/src/foundation/json-patch/inverse.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance apply-patch selection-headless`; `npm run build -w zod-crud`; focused 50k/10k runs showed `doc.patch escaped batch 10000 selection=single` p50 about 16.22ms before vs about 4.08-10.06ms after. |
| 117 | Escaped same-array nested replace batches still disabled the text-boundary index parser and fell back to full JSON Pointer parsing after the first op in local validation, accepted replay, and inverse generation. A direct 50k/10k pre-change check showed `doc.patch escaped nested field replace batch 10000` p50 about 13.09ms with history=0, 21.78ms with history=100, and inverse generation about 8.03ms. | Reused canonical JSON Pointer prefix/suffix text from the first nested replace op for later row-index parsing, including escaped array parents and escaped nested suffix fields. Added `perf:core` rows for escaped nested replace batches and a regression proving escaped nested patch/undo/redo avoid root schema validation under `strict: false`. | Performance regression evidence: 5 -> 5 with lower escaped nested replace overhead. Evidence: `packages/zod-crud/src/domain/schema/localPatch.ts`; `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/src/foundation/json-patch/inverse.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `scripts/benchmark-core.mjs`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance apply-patch`; `npm run build -w zod-crud`; focused `PERF_GC=1 PERF_ITEMS=50000 PERF_BATCH=10000 PERF_ROOT_KEYS=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=7 node --expose-gc scripts/benchmark-core.mjs` reported escaped nested p50 about 2.78ms history=0, 5.78ms history=100, and 2.23ms for `computeInverses`. |
| 118 | Large stable replace batches with 500 selected string pointers still built a 10k replacement `Set` before proving no selected pointer had a replaced strict ancestor. A focused 50k/10k run showed `doc.patch batch 10000 selection=multiple-500` p50 about 9.29ms while history=0 was about 2.74ms and single selection was about 3.84ms. | Added a multi-string-selection fast path that builds the much smaller strict-ancestor set from the current selection and scans replacement ops once. It keeps the existing fallback for URI fragments, non-string points, invalid pointer text, and very large ancestor sets. Added escaped multiple-selection regression coverage for exact leaf replacement plus ancestor replacement. | Performance regression evidence: 5 -> 5 with lower multiple-selection stable replace overhead. Evidence: `packages/zod-crud/src/domain/selection/index.ts`; `packages/zod-crud/tests/selection-headless.test.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- selection-headless document-history-performance`; `npm run build -w zod-crud`; focused `PERF_GC=1 PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=9 node --expose-gc scripts/benchmark-core.mjs` showed `doc.patch batch 10000 selection=multiple-500` p50 about 9.29ms before vs about 4.83ms after. |
| 119 | Large append batches with multiple-selection auto-targeting still rebuilt target pointer strings after accepted replay had already normalized `/-` to concrete paths. A focused 50k/10k run showed `doc.patch add batch 10000 selection=multiple` p50 about 11.77ms, with no-selection add batch p50 in the same area around 6-7ms. | Reused the concrete applied `op.path` strings for increasing add/copy auto-target batches instead of regenerating them from parsed indexes. Descending/non-increasing insert batches still compute final shifted indexes. Existing append auto-selection regressions cover plain and escaped array parents. | Performance regression evidence: 5 -> 5 with lower append auto-selection overhead. Evidence: `packages/zod-crud/src/domain/tracking/pointer.ts`; `packages/zod-crud/tests/selection-headless.test.ts`; `packages/zod-crud/test/track.test.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- selection-headless`; `npm test -w zod-crud -- track selection-headless`; `npm run build -w zod-crud`; focused `PERF_GC=1 PERF_ITEMS=50000 PERF_BATCH=10000 PERF_INDIVIDUAL=100 PERF_ROUNDS=9 node --expose-gc scripts/benchmark-core.mjs` showed `doc.patch add batch 10000 selection=multiple` p50 about 11.77ms before vs about 8.23ms after. |
| 120 | The increasing add/copy auto-target path still allocated and filled a numeric index array even though loop 119 changed the increasing case to return concrete applied paths. That array is only needed for descending/non-increasing insert batches that must compute final shifted indexes. | Made the index array lazy: increasing batches keep only the concrete path array; the numeric array is backfilled only when a non-increasing target is detected. Existing track and selection regressions cover the descending and append semantics. | Performance regression evidence: 5 -> 5 with lower helper-level allocation on append auto-targets. Evidence: `packages/zod-crud/src/domain/tracking/pointer.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- track selection-headless`; `npm run build -w zod-crud`; direct 10k helper comparison showed previous monotonic auto-target p50 about 2.05ms vs current `pickAutoTargetsInfo` p50 about 1.82ms. |
| 121 | Increasing add/copy auto-target parsing still rediscovered the array parent for every op. This is especially expensive for escaped array parents because `arrayElementLocation` decodes and rebuilds the pointer each time, even though the first op proves the encoded parent prefix. | Cached the encoded array-element prefix after the first parsed target and parsed only the numeric suffix for later monotonic insert targets. If a later path does not match the same encoded prefix, the path falls back instead of broadening semantics. Existing append auto-selection regressions cover escaped array parents. | Performance regression evidence: 5 -> 5 with lower escaped append auto-target overhead. Evidence: `packages/zod-crud/src/domain/tracking/pointer.ts`; `packages/zod-crud/tests/selection-headless.test.ts`; `packages/zod-crud/test/track.test.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- track selection-headless`; `npm run build -w zod-crud`; direct escaped 10k helper comparison showed loop-120-style auto-target p50 about 14.55ms vs current `pickAutoTargetsInfo` p50 about 0.93ms. |
| 122 | The root bulk retained-heap row stayed narrow and healthy on a fresh focused run (`history root object add redo then small patch retained heap 10000` p50 about 2.74MB, undo/redo about 0.06/0.04ms), so broadening snapshot lifetime was not justified. The same run exposed a smaller actionable path: `history: 1` repeatedly overflowed the mutable undo stack and compacted with `splice(0, 1)` on every commit even though only the newest entry can survive. | Added a `limit === 1` fast path in `commitMutable` that overwrites the single undo slot, resets `undoStart`, and clears redo without prefix compaction. Added a performance-contract regression that the backing undo array stays length 1 across repeated commits. | Performance regression evidence: 5 -> 5 with lower small-limit history churn. Evidence: `packages/zod-crud/src/foundation/history.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance`; `npm run build -w zod-crud`; direct `doc.patch` 50k replace with `history=1` p50 about 160.13ms before vs about 78.36ms after; isolated 500k `commitMutable(limit=1)` old-style p50 about 16.72ms vs current p50 about 13.47ms. |
| 123 | Root-object remove/replace history inverse generation still used a `Set` to reject duplicate root keys. For 10k root batches this is pure duplicate-tracking overhead on top of the required object lookups and inverse allocation; the add path was measured separately and did not benefit from the same change. | Switched root-object remove/replace inverse duplicate tracking from `Set` to a null-prototype seen-key map, preserving special data keys such as `__proto__` and leaving root-object add inverses unchanged. Existing repeated-key and special-key history regressions cover the fallback and safety behavior. | Performance regression evidence: 5 -> 5 with lower root inverse bookkeeping. Evidence: `packages/zod-crud/src/foundation/json-patch/inverse.ts`; existing `packages/zod-crud/tests/document-history-performance.test.ts` root repeated-key and special-key coverage; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance apply-patch`; `npm run build -w zod-crud`; direct 10k inverse comparison showed root remove p50 about 5.13ms old vs 2.80ms current and root replace p50 about 2.93ms old vs 2.18ms current. A focused root benchmark kept undo/redo snapshot rows under 0.1ms and reported `doc.patch root object remove batch 10000 history=100` p50 about 7.19ms. |
| 124 | Repeated JSONPath queries still refreshed the 128-entry parse-cache LRU on every hit with `Map.delete` plus `Map.set`. Hot UI paths often call the same JSONPath string repeatedly, so this made cached simple-path queries pay bookkeeping unrelated to parsing or evaluation. | Added a one-entry last-hit cache in front of the existing LRU map. The LRU behavior stays in place for non-last hits and misses, while repeated calls to the same query string return the already cached AST without mutating the map. Existing JSONPath query, read, and check tests cover behavior. | Performance regression evidence: 5 -> 5 with lower repeated JSONPath cache-hit overhead. Evidence: `packages/zod-crud/src/foundation/jsonpath/index.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- jsonpath jsonpath-fast-path document-check document-read`; `npm run build -w zod-crud`; direct 50k-state `$.items[25000].done` cached-query loop showed old-style p50 about 3.53ms vs current exported query p50 about 2.91ms for 10k calls. Focused core rows stayed healthy: `doc.query jsonpath wildcard` p50 about 3.83ms and `jsonpath queryMatches wildcard field` p50 about 4.77ms. |
| 125 | Direct JSONPath parsing still tokenized common simple paths such as `$.items[25000].done` on every call. This matters for cold query checks, `canFind`, and callers that intentionally use `parse()` instead of the cached `query()` helper. | Added a narrow parser fast path for `$` followed by ASCII dot names, dot/bracket wildcards, and safe integer bracket indexes. All other JSONPath syntax still falls through to the existing tokenizer/parser, preserving full RFC behavior. Added fast-path AST regression coverage. | Performance regression evidence: 5 -> 5 with lower simple JSONPath parse overhead. Evidence: `packages/zod-crud/src/foundation/jsonpath/parser.ts`; `packages/zod-crud/tests/jsonpath-fast-path.test.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- jsonpath jsonpath-fast-path jsonpath-cts document-check document-read`; `npm run build -w zod-crud`; direct 50k-state `parse+evaluate` for `$.items[25000].done` 10k calls showed prototype old p50 about 23.87ms vs current dist p50 about 5.49ms. Focused core `jsonpath direct parse+evaluate 10000` p50 was about 4.86ms, with cached query p50 about 3.41ms. |
| 126 | The JSONPath array regex-filter fast path still used `RegExp.test` for literal patterns like `search(@.title, "999")`. For these patterns, RFC regexp search is equivalent to a substring check and full match is equivalent to string equality, while metacharacter patterns must remain on the regex path. | Added literal-pattern detection for the array regex-filter fast path. Patterns containing regex metacharacters still compile and run through the existing regex cache; plain literal search uses `includes`, and plain literal match uses strict equality. Added regressions for literal `search`, literal `match`, and metacharacter `.` fallback behavior. | Performance regression evidence: 5 -> 5 with lower literal filter scan overhead. Evidence: `packages/zod-crud/src/foundation/jsonpath/evaluate.ts`; `packages/zod-crud/tests/jsonpath-fast-path.test.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- jsonpath jsonpath-fast-path jsonpath-cts document-check document-read`; `npm run build -w zod-crud`; direct 50k scan body comparison showed regex p50 about 2.09ms vs literal includes p50 about 1.97ms, and focused core `jsonpath regex filter search` p50 was about 2.23ms after the prior focused row was in the 4ms range. |
| 127 | The plain-object JSON boundary success path still created both `Object.keys()` and `Object.getOwnPropertyNames()` arrays for every object, even though the descriptor scan can detect non-enumerable properties from the own-name list alone. This showed up in large external JSON boundaries where the descriptor checks must stay in place. | Switched the fast serializability check and fast clone validation object branches to scan `Object.getOwnPropertyNames()` once and reject non-enumerable descriptors in the same loop. Symbol, accessor, non-plain-object, and detailed-error fallbacks remain unchanged. | Performance regression evidence: 5 -> 5 with slightly lower object-boundary bookkeeping and unchanged JSON safety. Evidence: `packages/zod-crud/src/foundation/json.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- create-json-document apply-patch`; `npm run build -w zod-crud`; same-process 50k direct comparison showed old-style object scan p50 about 29.98ms vs current 28.84ms on the first pass, with a repeated pass neutral around 28.7-28.9ms; focused core rows kept public JSON boundaries in the same range. |
| 128 | Source-order full root-object remove batches still fell out of the cheap suffix path and built duplicate-tracking state even though `Object.keys(state)` already proves the exact key order. Ordered full root-object replace batches also repeated `hasOwnProperty` checks after matching the source key at the same index. | Added a source-key-order fast path for full root-object remove replay and skipped redundant ownership checks on ordered full root-object replace replay. The existing reverse-suffix path remains separate, and a duplicate-remove regression caught and guarded against mixing the two fast paths. | Performance regression evidence: 5 -> 5 with lower root-object bulk replay overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance apply-patch`; `npm run build -w zod-crud`; same-process 10k helper comparison showed source-order full remove p50 about 4.12ms old vs 1.32ms current, and source-order full replace p50 about 5.04ms old vs 3.51ms current. Focused core rows reported `accepted root object remove batch 10000` p50 about 1.81ms and `trusted root object remove batch 10000` p50 about 1.06ms. |
| 129 | Root-record local validation still built duplicate-tracking state before handling source-order full remove batches. Loop 128 made accepted replay cheap, but `doc.patch root object remove batch 10000` still paid local-validation bookkeeping before history could use the fast replay path. | Added a source-order full root-record remove fast path in local schema validation. It validates the remove op shape and source key order up front, returns the empty root directly, and leaves partial, duplicate, escaped, and non-source-order removes on the existing duplicate-tracked path. Existing duplicate-remove and source-order history regressions cover the split. | Performance regression evidence: 5 -> 5 with lower root-record remove commit overhead. Evidence: `packages/zod-crud/src/domain/schema/localPatch.ts`; existing `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance apply-patch`; `npm run build -w zod-crud`; focused 50k/10k core rows showed `doc.patch root object remove batch 10000 history=0` p50 about 2.85ms before vs 1.81ms after, and `history=100` p50 about 4.85ms before vs 3.02ms after. |
| 130 | Ordered full root-record replace batches still copied the entire root record and then overwrote every key during local schema validation. The foundation replay path already handled ordered full replace directly, so `doc.patch root object replace batch 10000` was paying an avoidable copy before history recording. | Added a source-order full root replace path inside local schema validation. It keeps per-value JSON/schema validation, preserves `__proto__` as data, falls back to the copied-root path on any order mismatch, and adds a history restore regression for source-order full root record replace. | Performance regression evidence: 5 -> 5 with lower root replace commit overhead. Evidence: `packages/zod-crud/src/domain/schema/localPatch.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance apply-patch`; `npm run build -w zod-crud`; focused 50k/10k core rows showed `doc.patch root object replace batch 10000 history=0` p50 about 6.38ms before vs 5.35ms after, and `history=100` p50 about 8.83ms before vs 6.90ms after. |
| 131 | Root-record add local validation copied the root record as soon as the first op passed validation, then continued validating the remaining 10k add values while holding the copied root. If a later value failed, that copy was wasted; on the success path it also interleaved validation work with root writes. | Split root-record add local validation into a validation pass and an apply pass. The first pass keeps existing op shape, JSON, and value-schema checks; the second pass copies the root once and applies the already accepted adds in order, preserving object-add replacement semantics and `__proto__` data keys. | Performance regression evidence: 5 -> 5 with lower root-add commit overhead and less wasted work on failed batches. Evidence: `packages/zod-crud/src/domain/schema/localPatch.ts`; existing root add and `__proto__` history regressions; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance apply-patch`; `npm run build -w zod-crud`; focused 50k/10k core rows showed `doc.patch root object add batch 10000 history=100` p50 about 11.62ms before vs 9.92ms after, with `history=0` staying in the same range around 7.7ms. |
| 132 | Single-field suffix rekey still visited leaf records that cannot contain the requested field. In the 50k item benchmark, every `meta` object has no `id` and no object-valued children, so recursive visits there could not affect suffix conflict detection but still ran during duplicate/paste rekey scans. | Added a single-field text walker that skips record children with neither the target field nor object-valued descendants. Arrays and nested records that may contain the field still recurse, so nested `id` rekey behavior is preserved. | Performance regression evidence: 5 -> 5 with lower suffix rekey scan overhead. Evidence: `packages/zod-crud/src/domain/schema/rekey.ts`; existing nested rekey coverage in `packages/zod-crud/tests/create-json-document.test.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- create-json-document document-history-performance`; `npm run build -w zod-crud`; focused 50k benchmark showed `doc.clipboard.pastePayload spread 100 + rekey` p50 about 4.14ms before vs 3.69ms after, while `doc.canDuplicate middle + rekey` stayed about 2.04ms. |
| 133 | Replaying many individual history entries still routed each unescaped `/items/{i}/done` replace through full JSON Pointer parsing and generic parent-chain mutation. This is a hot path for large undo/redo made from many small edits, while batch and transaction undo already avoid most of that cost. | Added a trusted single-value mutation fast path for the common object-array-object-field shape. It handles unescaped three-segment paths before the generic parser, then falls back for escaped, deeper, non-array, or non-object cases. | Performance regression evidence: 5 -> 5 with lower small-entry history replay overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- document-history-performance apply-patch create-json-document`; `npm run build -w zod-crud`; focused 50k benchmark showed `history undo individual 100 entries` p50 about 11.45ms before vs 4.63ms after on the same noisy row, and same-process wrapper comparison showed current replay around 4.13-5.37ms p50 vs old generic wrapper around 4.24-5.61ms p50. |
| 134 | Many single append edits still routed each `/items/-` patch through batch-oriented trusted replay candidates after local validation. In a fresh 50k run, `doc.patch individual add 100 history=0` p50 was about 13.37ms, much higher than individual replace/remove and the append batch path. | Added a single-op trusted value fast path before batch replay candidates and a narrow unescaped root object -> array element mutation path for `/items/{index}` add/replace. Escaped, deeper, root-array, and non-array paths keep the generic fallback. | Performance regression evidence: 5 -> 5 with lower individual append overhead. Evidence: `packages/zod-crud/src/foundation/json-patch/index.ts`; `npm run typecheck -w zod-crud`; `npm test -w zod-crud -- apply-patch document-history-performance create-json-document`; `npm run build -w zod-crud`; focused 50k benchmark showed `doc.patch individual add 100 history=0` p50 about 13.37ms before vs about 5.25-5.89ms after, with individual replace/undo rows stable around 4.5-4.7ms on the repeat run. |
| 135 | The final local 1.0 gate was still a memory checklist: root `verify`, `perf:core`, and tarball packing were documented in different places, so a release could pass normal verification without recording the current performance run or package artifact creation. | Added root `release:check` to run `verify`, `perf:core`, and `pack:library` in order; documented it in README, SPEC, site API docs, `llms.txt`, and release notes; taught docs consistency and `docs:evaluate` to reject missing `release:check` drift. | Runtime health: 5 -> 5 for release gating. Performance regression evidence: stronger because the final local gate now includes `perf:core`. Evidence: `package.json`; `packages/zod-crud/README.md`; `packages/zod-crud/SPEC.md`; `apps/site/src/docs/zod-crud-api.md`; `llms.txt`; `docs/release-notes.md`; `scripts/evaluate-docs.mjs`; `apps/site/tests/docs-consistency.test.ts`. |
| 136 | The browser part of the release gate could silently reuse any existing local server on the default port. A failed `release:check` run hit an unrelated app titled `ppt`, proving the gate was not isolating the demo smoke target. | Moved the default browser-test port away from common Vite ports, changed Playwright to start a strict-port dev server by default, and reuse an existing server only when `PLAYWRIGHT_REUSE_EXISTING_SERVER=1` is explicitly set; added a docs evaluator guard for the strict fresh-server policy. | Runtime health: 5 -> 5 for browser smoke determinism. Evidence: `playwright.config.ts`; `scripts/evaluate-docs.mjs`; failed run context showed `PPT Retouch` before the config change. |
| 137 | The locked public export list was duplicated in package smoke, docs evaluation, and site docs consistency tests. That made the 1.0 API lock vulnerable to tests drifting apart while still passing locally. | Added `packages/zod-crud/public-contract.json` as the public export contract SSOT; package smoke, docs evaluation, and docs consistency now read the same root/react value/type export lists; SPEC and release notes document the SSOT. | API correctness evidence: stronger for the 1.0 public API lock. Evidence: `packages/zod-crud/public-contract.json`; `packages/zod-crud/test/package-smoke.mjs`; `scripts/evaluate-docs.mjs`; `apps/site/tests/docs-consistency.test.ts`; `packages/zod-crud/SPEC.md`; `docs/release-notes.md`. |
| 138 | The final local gate was `release:check`, but package `prepublishOnly` still ran only package-local `verify`. A manual `npm publish -w zod-crud` could therefore skip docs drift, browser demo smoke, performance measurement, and final tarball packing. | Changed package `prepublishOnly` to delegate to root `release:check`; updated `llms.txt` and release notes; added docs evaluator and docs consistency guards so publish lifecycle drift is caught. | Runtime health: 5 -> 5 for publish gating. Evidence: `packages/zod-crud/package.json`; `scripts/evaluate-docs.mjs`; `apps/site/tests/docs-consistency.test.ts`; `llms.txt`; `docs/release-notes.md`. |

## Next Candidates

- Keep the root bulk history snapshot path narrow. It is for large root-level add/remove/replace batches only; do not broaden it to nested object or array batches without a separate memory-retention audit.
- Use the retained-heap row before changing snapshot lifetime again; p50 restore speed alone is no longer enough evidence for root bulk history changes.
- Keep small-limit history changes targeted. A broad ring-buffer rewrite is not justified by current evidence; `limit=1` now has the only proven fast path.
- Treat URI fragments and non-string selection points as fallback-only for stable replace tracking unless a focused workload proves they matter.
- Escaped nested replace batches now cover canonical `~0`/`~1` pointer text. Do not tune URI-fragment or noncanonical pointer text fallback without a focused workload showing it matters.
- Treat further trusted-clone tuning skeptically. A separate guarded/unguarded function split looked cleaner locally but made `doc.clipboard.write items payload trusted` p50 unstable around 7.31ms, so the smaller per-clone guard is the current safer shape.
- Treat rekey scanner callback removal skeptically. The generic walker is not the bottleneck for spread rekey; conflict-set semantics and payload clone/preflight dominate the remaining path.
- Keep public `applyPatch` and external clipboard writes as explicit JSON boundaries. Current 50k evidence keeps them expensive by design: public `applyPatch` repeated item replace batch 10k p50 about 54.30ms, `jsonSerializableError` p50 about 31.02ms, and external `clipboard.write` p50 about 37.21ms.
