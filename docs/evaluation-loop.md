# Evaluation Loop Ledger

Goal: run 100 evaluation -> execution -> scoring loops for the zod-crud docs and demos before release.

This ledger is the progress record. A loop only counts when all three parts are present:

- Evaluate: name the user-facing confusion or missing proof.
- Execute: change docs, demo, tests, or verification assets toward that finding.
- Score: record current evidence after the change.

## Score Axes

Each loop scores only the axis it tries to improve.

| Axis | Passing evidence |
| --- | --- |
| API correctness | Docs/demo use the current public API and avoid private modules or stale names. |
| Target clarity | Raw insertion pointers and `{ before | after | replace }` value-relative targets are not mixed. |
| Task discoverability | A user can find the action needed to verify a real editing task. |
| State observability | Selection, clipboard, result, state, and patch effects are inspectable after each action. |
| LLM copyability | An LLM can copy an example without adding commit-after-mutation or `{ at }` mistakes. |
| Runtime health | Typecheck, tests, build, or browser checks cover the changed surface. |
| Performance regression | Core editing workloads have repeatable local measurements and targeted tests for optimized paths. |

## Progress

27 / 100 loops complete.

| Loop | Evaluate | Execute | Score |
| --- | --- | --- | --- |
| 001 | The workbench used one `target` for both existing values and insertion positions. That made `paste({ after: target })`, `paste("/cards/-")`, schema insert checks, and move destinations hard to distinguish. Clipboard state was also only `set/empty`, so repeated copy/paste behavior could not be graded from the UI. | Split workbench state into `valueTarget` and `insertTarget`; routed value-relative actions through `{ after: valueTarget }`; routed insertion actions through raw `insertTarget`; added clipboard snapshot and result call panels; added copy-to-insert and payload-insert actions. | Target clarity: 2 -> 4. State observability: 2 -> 4. Evidence: `apps/site/src/playgrounds/InterfaceWorkbench.playground.tsx`; `npm run typecheck -w @zod-crud/site` passed. |
| 002 | The docs explained each facade, but did not give a compact domain action layer that combines `can*`, patch, duplicate, selection-derived copy, paste targets, and history. That leaves LLMs and users to infer composition rules. | Added a `작업 레이어 예시` section to the site API doc with `addCard`, `duplicateCard`, `copySelectedCardsTo`, `pastePayloadAfter`, and `undo`, including raw insertion pointer vs `{ after }` wording. | LLM copyability: 3 -> 4. API correctness: 4 -> 4. Evidence: `apps/site/src/docs/zod-crud-api.md`; `npm run typecheck -w @zod-crud/site`, `npm test -w @zod-crud/site`, `npm run build -w @zod-crud/site`, and `git diff --check` passed. |
| 003 | The workbench target split needed a regression guard. Without a test, future UI cleanup could collapse value and insertion targets back into one ambiguous control. | Updated the site component test to require accessible `value target` and `insert target` controls and to execute the `copy to insert` flow. | Runtime health: 3 -> 4. Target clarity evidence: stronger. Evidence: `apps/site/tests/interface-workbench.test.tsx`; `npm run typecheck -w @zod-crud/site`, `npm test -w @zod-crud/site`, `npm run build -w @zod-crud/site`, and `git diff --check` passed. |
| 004 | The workbench result panel used short labels such as `patch.add` and `clipboard.paste`, so it showed that an action ran but not the public API shape a user or LLM should copy. | Changed result `call` values across the workbench to public API snippets such as `doc.clipboard.paste({ after: pointer })`, `doc.clipboard.paste(pointer, { spread: true, rekey })`, `doc.canPastePayload(pointer, payload)`, and `doc.schema.accepts(pointer, payload, "insert")`; updated component tests to assert those snippets appear after representative actions. | LLM copyability: 4 -> 5 for demo actions. Evidence: `apps/site/src/playgrounds/InterfaceWorkbench.playground.tsx`; `apps/site/tests/interface-workbench.test.tsx`; `npm run typecheck -w @zod-crud/site` and `npm test -w @zod-crud/site -- interface-workbench` passed. |
| 005 | Users and LLMs still had to infer JSONPath dialect boundaries from examples. Prior blind results showed uncertainty around query syntax and whether JSONPath participates in mutation. | Added canonical JSONPath examples and an explicit non-goal: JSONPath finds values; patches are still built with returned JSON Pointers. Mirrored the note in the README and guarded the site doc text in the workbench test. | LLM copyability: 4 -> 5 for query-to-patch flow. API correctness evidence: stronger. Evidence: `apps/site/src/docs/zod-crud-api.md`; `packages/zod-crud/README.md`; `apps/site/tests/interface-workbench.test.tsx`; `npm run typecheck -w @zod-crud/site` and `npm test -w @zod-crud/site -- interface-workbench` passed. |
| 006 | Selection docs said selection is state, but did not clearly show how a multi-select becomes a clipboard source or when clipboard methods can use selection defaults. This left source ownership ambiguous. | Added site and README examples that derive `source` from `doc.selection?.selectedPointers`, pass it to `doc.clipboard.copy(source)` and `cut(source)`, and state that omitted source falls back to current selection while explicit sources are easier to audit. Guarded the site wording in component tests. | Task discoverability: 3 -> 4. State observability: 4 -> 4. Evidence: `apps/site/src/docs/zod-crud-api.md`; `packages/zod-crud/README.md`; `apps/site/tests/interface-workbench.test.tsx`; `npm test -w @zod-crud/site` passed. |
| 007 | Capability docs showed success/failure shape but did not show how schema validation failures become UI-ready messages. This made `canPastePayload` less useful as a preflight interface. | Added failed `canPastePayload` examples to the site doc and README using `schema_violation` and `violations`, and guarded the site example in the workbench test. | Task discoverability: 4 -> 5 for validation flows. API correctness evidence: stronger. Evidence: `apps/site/src/docs/zod-crud-api.md`; `packages/zod-crud/README.md`; `apps/site/tests/interface-workbench.test.tsx`; `npm test -w @zod-crud/site` passed. |
| 008 | Paste target and mutation result semantics are repeated across README, SPEC, site docs, and `llms.txt`. Without an automated consistency check, stale aliases like `{ at }` or `PasteMode` can re-enter one surface unnoticed. | Added a site docs consistency test that reads README, SPEC, site docs, and `llms.txt`, rejects legacy paste target aliases, requires current `{ after }` wording, and requires applied-result "do not commit again" semantics. Updated site Vitest include rules so `.test.ts` consistency tests actually run. | API correctness evidence: 4 -> 5 for docs consistency. Runtime health: 4 -> 5 for docs drift. Evidence: `apps/site/tests/docs-consistency.test.ts`; `apps/site/vitest.config.ts`; `npm test -w @zod-crud/site` passed with 2 files and 4 tests. |
| 009 | The docs were still organized mostly by API surface. A first-time user with a concrete task had to translate "I want to paste a payload" or "I want to undo" into the right facade. | Added task-entrypoint tables to the site doc and README mapping common editing tasks to `doc.patch`, `doc.duplicate`, `doc.query`, `selection -> clipboard`, `pastePayload`, `can*`, and history calls. Guarded the site table in component tests. | Task discoverability: 4 -> 5. LLM copyability: 5 -> 5. Evidence: `apps/site/src/docs/zod-crud-api.md`; `packages/zod-crud/README.md`; `apps/site/tests/interface-workbench.test.tsx`; `npm run typecheck -w @zod-crud/site`, `npm test -w @zod-crud/site`, `npm run build -w @zod-crud/site`, and `git diff --check` passed. |
| 010 | JSONPath boundaries were improved in README and site docs, but SPEC and `llms.txt` did not yet carry the same explicit "search only, patch with Pointer" rule. | Added the JSONPath search-only rule to SPEC and `llms.txt`, then extended the docs consistency test to require this rule across README, SPEC, site docs, and `llms.txt`. | API correctness evidence: 5 -> 5 for query/mutation boundary. Runtime health: 5 -> 5 for docs drift. Evidence: `packages/zod-crud/SPEC.md`; `llms.txt`; `apps/site/tests/docs-consistency.test.ts`; `npm run typecheck -w @zod-crud/site`, `npm test -w @zod-crud/site`, `npm run build -w @zod-crud/site`, and `git diff --check` passed. |
| 011 | The 100-loop ledger itself had no automated integrity check. As the loop count grows, progress text, row count, or unverified score text could drift. | Added `scripts/evaluate-docs.mjs` and the root `npm run docs:evaluate` script. The script checks ledger progress vs row count, rejects unverified rows, and scans docs for stale paste target aliases plus required task/JSONPath guidance. | Runtime health: 5 -> 5 for loop accounting. Evidence: `scripts/evaluate-docs.mjs`; `package.json`; `npm run docs:evaluate` passed. |
| 012 | The docs evaluation command was separate from the main verification path, so release verification could still pass while the loop ledger or docs consistency failed. | Added `npm run docs:evaluate` to the root `verify` script after package smoke. | Runtime health: 5 -> 5 for release gating. Evidence: `package.json`; `npm run docs:evaluate` passed. |
| 013 | The site doc was authored as Markdown, but the workbench Markdown viewer flattened tables into pipe-text paragraphs. The task-entrypoint table existed in source but did not read like a doc page in the demo. | Added Markdown table parsing and rendering to the workbench viewer and guarded table rendering in the workbench test. | Task discoverability: 5 -> 5 with stronger demo evidence. Evidence: `apps/site/src/playgrounds/InterfaceWorkbench.playground.tsx`; `apps/site/tests/interface-workbench.test.tsx`; `npm test -w @zod-crud/site` passed. |
| 014 | The demo's `doc.can*` group did not expose the whole public capability family. `canPatch`, `canRemove`, `canMove`, `canDuplicate`, `canCut`, and `canPaste` were missing from runnable UI even though docs listed them. | Added buttons for all missing capability methods and result snippets that show the exact public calls. | Task discoverability: 5 -> 5 for capability checks. API correctness evidence: stronger. Evidence: `apps/site/src/playgrounds/InterfaceWorkbench.playground.tsx`; `apps/site/tests/interface-workbench.test.tsx`; `npm test -w @zod-crud/site` passed. |
| 015 | A few workbench result labels still used internal shorthand such as `patch.remove` and `query.select`, which weakened the demo as a copyable external-interface reference. | Replaced those labels with public-call snippets, including `doc.patch(selectedPointers.map(...))` and `doc.query(jsonPath); doc.selection?.selectRanges(pointers)`. | LLM copyability: 5 -> 5 with less ambiguous result output. Evidence: `apps/site/src/playgrounds/InterfaceWorkbench.playground.tsx`; `npm test -w @zod-crud/site` passed. |
| 016 | The human public export lists did not match the root export surface. Important exported types such as `JSONDocumentChangeListener`, `JSONDocumentCommitOptions`, `JSONDocumentCommitSelection`, and `JSONDocumentLoadOptions` were missing from docs. | Completed the README and site public export snippets and added docs consistency coverage for every documented root export name across README, SPEC, and site docs. | API correctness evidence: 5 -> 5 for public export completeness. Evidence: `packages/zod-crud/README.md`; `apps/site/src/docs/zod-crud-api.md`; `apps/site/tests/docs-consistency.test.ts`; `npm test -w @zod-crud/site` passed. |
| 017 | SPEC's top-level concept map omitted `duplicate(pointer, options)`, even though duplicate is a first-class public document action in README, site docs, and `llms.txt`. | Added `duplicate(pointer, options)` to the SPEC concept map and made `docs:evaluate` require it. | API correctness evidence: stronger for the public concept map. Evidence: `packages/zod-crud/SPEC.md`; `scripts/evaluate-docs.mjs`; `npm run docs:evaluate` passed. |
| 018 | Release verification guidance was split: root `verify` now ran docs evaluation, but README, site docs, SPEC, and `llms.txt` did not consistently say that docs drift and the loop ledger are release-gated. | Added verification guidance with `docs:evaluate` to README, site docs, SPEC, and `llms.txt`, then added docs consistency and evaluator checks. | Runtime health: 5 -> 5 for release documentation. Evidence: `packages/zod-crud/README.md`; `apps/site/src/docs/zod-crud-api.md`; `packages/zod-crud/SPEC.md`; `llms.txt`; `apps/site/tests/docs-consistency.test.ts`; `scripts/evaluate-docs.mjs`; `npm test -w @zod-crud/site` and `npm run docs:evaluate` passed. |
| 019 | The package smoke test had a useful compile-time rejection for legacy `{ at }` paste targets, but the docs evaluator did not require that negative guard to stay in place. | Extended `docs:evaluate` to check the package smoke script for the `@ts-expect-error` guard proving `{ at }` is intentionally not a public paste target. | Runtime health: 5 -> 5 for legacy-alias regression coverage. Evidence: `scripts/evaluate-docs.mjs`; `packages/zod-crud/test/package-smoke.mjs`; `npm run docs:evaluate` passed. |
| 020 | Selection docs still showed only a small multi-select example. They did not give a compact map of the actual selection read, movement, text-planning, serialization, and subscription surface. | Added selection surface tables to README and site docs and guarded `selectedPointers`, `textPatch`, and `restore(snapshot)` in docs consistency and `docs:evaluate`. | State observability: 4 -> 5 for selection docs. Task discoverability evidence: stronger. Evidence: `packages/zod-crud/README.md`; `apps/site/src/docs/zod-crud-api.md`; `apps/site/tests/docs-consistency.test.ts`; `scripts/evaluate-docs.mjs`; `npm test -w @zod-crud/site` and `npm run docs:evaluate` passed. |
| 021 | History docs showed undo, redo, and transaction, but not enough detail about serializable metadata, `mergeKey`, explicit final selection, or `mergeLast`. | Added history metadata examples to README and site docs and guarded `mergeKey` plus `mergeLast` in docs consistency and `docs:evaluate`. | State observability: 5 -> 5 for history metadata. LLM copyability evidence: stronger. Evidence: `packages/zod-crud/README.md`; `apps/site/src/docs/zod-crud-api.md`; `apps/site/tests/docs-consistency.test.ts`; `scripts/evaluate-docs.mjs`; `npm test -w @zod-crud/site` and `npm run docs:evaluate` passed. |
| 022 | The package had many source-entry tests and a package smoke, but the smoke runtime path did not exercise a realistic installed-package document workflow across query, selection, clipboard, duplicate, can*, and history. | Extended `package-smoke.mjs` with an installed `zod-crud` public-interface scenario that imports from the package entrypoint and runs query-to-selection, clipboard spread paste, duplicate with rekey, `canCopy`, `canUndo`, and undo. Added an evaluator check that this scenario remains present. | Runtime health: 5 -> 5 for external-interface coverage. API correctness evidence: stronger. Evidence: `packages/zod-crud/test/package-smoke.mjs`; `scripts/evaluate-docs.mjs`; `npm run smoke:package -w zod-crud` and `npm run docs:evaluate` passed. |
| 023 | The workbench test clicked representative actions, but did not assert that each public facade group kept all expected controls visible. A future demo edit could silently drop a method from the interface map. | Added a control-discoverability test that checks the expected button list for `doc.patch`, document actions, selection, clipboard, history, query, `can*`, schema, and pure exports. | Runtime health: 5 -> 5 for demo interface coverage. Task discoverability evidence: stronger. Evidence: `apps/site/tests/interface-workbench.test.tsx`; `npm test -w @zod-crud/site` passed. |
| 024 | Markdown table rendering was guarded by the component test, but the release docs evaluator did not know that table rendering is part of the API docs demo contract. | Added `docs:evaluate` checks for workbench table block support, `<table>` rendering, and the table role assertion in the workbench test. | Runtime health: 5 -> 5 for docs demo release gating. Evidence: `scripts/evaluate-docs.mjs`; `npm run docs:evaluate` passed. |
| 025 | `JSONDocument<T>` exposes `canFind(jsonpath)`, but README, site docs, `llms.txt`, and the demo `can*` controls omitted it. That made the can* family non-MECE and hid query validation. | Added `canFind` to README, site docs, `llms.txt`, the workbench `doc.can*` group, component tests, docs consistency tests, and `docs:evaluate`. | API correctness: 5 -> 5 with a real public-interface omission fixed. Task discoverability evidence: stronger. Evidence: `packages/zod-crud/README.md`; `apps/site/src/docs/zod-crud-api.md`; `llms.txt`; `apps/site/src/playgrounds/InterfaceWorkbench.playground.tsx`; `apps/site/tests/docs-consistency.test.ts`; `apps/site/tests/interface-workbench.test.tsx`; `scripts/evaluate-docs.mjs`; `npm run typecheck -w @zod-crud/site`, `npm test -w @zod-crud/site`, and `npm run docs:evaluate` passed. |
| 026 | The final source layout changed the mental model, but release-facing docs did not yet share one SSOT for root entrypoints, internal concept layers, and the removed extra public layer plus React indirection. | Added final release notes, documented the source-layout SSOT across README, SPEC, site docs, `llms.txt`, and the API usage gap ledger, then made docs consistency and `docs:evaluate` reject stale source-layout paths. | API correctness evidence: stronger for import boundaries. Runtime health: 5 -> 5 for source-layout drift. Evidence: `docs/release-notes.md`; `packages/zod-crud/README.md`; `packages/zod-crud/SPEC.md`; `apps/site/src/docs/zod-crud-concepts.md`; `apps/site/src/docs/zod-crud-api.md`; `llms.txt`; `docs/api-usage-gaps.md`; `apps/site/tests/docs-consistency.test.ts`; `scripts/evaluate-docs.mjs`; `npm test -w @zod-crud/site`, `npm run docs:evaluate`, and `npm run verify` passed. |
| 027 | Large core editing workloads had no repeatable local measurement, and history undo/redo revalidated the whole Zod schema even though entries were generated from already accepted patches. That made large batch replay slower than necessary and left future performance work without a baseline. | Added `scripts/benchmark-core.mjs` plus `npm run perf:core`; added an internal trusted history replay path that keeps patch atomicity but skips schema revalidation for undo/redo; added a regression test proving undo/redo does not re-run whole-schema validation. | Performance regression: 0 -> 2. Runtime health evidence: stronger for history replay. Evidence: `scripts/benchmark-core.mjs`; `packages/zod-crud/src/foundation/json-patch/index.ts`; `packages/zod-crud/src/application/document/createJSON.ts`; `packages/zod-crud/src/application/document/createJSONDocument.ts`; `packages/zod-crud/tests/document-history-performance.test.ts`; `npm run typecheck -w zod-crud`, targeted history tests, and `npm run perf:core` passed. Current benchmark snapshot: 10k item undo/redo batch 1000 around 3ms; 50k item undo/redo batch 1000 around 75-80ms; individual 100 patches on 50k items around 11s. |

## Next Candidates

| Loop | Candidate |
| --- | --- |
| 028 | Add README smoke coverage for the new selection and history snippets without making docs too heavy. |
| 029 | Re-run a blind API usage task against only site docs and record pass/fail patterns. |
| 030 | Measure and reduce the remaining large-doc cost of repeated individual `doc.patch` calls. |
